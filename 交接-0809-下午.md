# 舞镜 · 交接文档 0809 下午场

**接手状态：** 8.8/10（上午交接）  
**离手状态：** 9.5/10 ✅ + 新功能「过一遍剧本」  
**最新 commit：** `e9da9e7` · branch: `auto-decompose-mediapipe-0730`

---

## 今下午做了什么

### 1. P2 TIER 1-4 全量完成 → 9.5/10

| TIER | 内容 | commit |
|------|------|--------|
| TIER 1 | `will-change` GPU优化（7个元素） | `4f629ea` |
| TIER 2 | 间距规范化（0异常残留） | `4f629ea` |
| TIER 3 | 字体梯度系统（7个CSS变量） | `4f629ea` |
| TIER 4 | WCAG AA 对比度修复（全部≥4.5:1） | `4eefde7` |

### 2. 上线前自检 20条 全绿 ✅

所有 og 标签、CSS 变量、catch 提示、容器显示逻辑、超时机制均通过。

### 3. 数据质量审查：拆解内容

**结论：动作描述内容质量 OK**，发现 2 个问题：
- 口诀格式 bug（`舞蹈1` 第5句用了 `-` 而非 `—`）→ 已修 retry 逻辑
- `舞蹈1/2/3`（3支）角度数据全空 → 需重跑 pose 阶段（见下）

### 4. Prompt 问题诊断 + 修复

| 问题 | 修复 |
|------|------|
| 口诀 retry 逻辑反了（检测 `—` 而不是 `-`） | ✅ 已修：现在只对连字符 `-` 重试 |
| 口诀格式两套标准（prompt 说无分隔符，实际出破折号）| ✅ 统一为破折号 `—`，retry 提示一致 |

### 5. 新功能：「过一遍剧本」`runthrough` 字段

**背景：** 现有逐句卡片是孤立的，演员读完一句要重新启动下一句，没有流动感。

**方案：** 调用 Claude 生成连贯过一遍剧本，用「接着/随之/紧接着」串联全程，演员读完闭眼能想象整支舞的流动。

**实现：**
- 新增 `_claude_runthrough(phrases, title, genre)` in `auto_decompose.py`
- 调用 `claude-sonnet-4-6`，古典舞用意象语言，K-pop 用节奏语言，150-220字
- `ANTHROPIC_API_KEY` 缺失时优雅降级（返回空串，不阻塞）
- 产物写入 `decompose.json["runthrough"]`

---

## Karen 还需要做的 1 件事

**服务器加 API Key**，SSH 进去加到 `/www/wujing-api/start.sh`：

```bash
export ANTHROPIC_API_KEY="sk-ant-你的key"
```

加完后，新上传视频拆解时会自动生成 `runthrough` 字段。

---

## 下一步：前端展示 `runthrough`

`runthrough` 字段已在后端生成，前端还没展示。

**在哪加：** `design-upgrade.html` 的故事卡区域，紧跟在 `story.body` 下方。

**建议 UI：**
```
┌────────────────────────────────┐
│ 📖 过一遍剧本                   │
│                                │
│ 站定，身体侧拧面向前，右手缓缓   │
│ 抬至肩高做兰花指，带笑目视前方   │
│ ——接着松肩，双臂从体侧柔和下落  │
│ ……                             │
└────────────────────────────────┘
```

代码参考（在渲染 story 卡的地方加）：
```javascript
if (d.runthrough) {
  html += `<div class="runthrough-card">
    <div class="rt-label">📖 过一遍剧本</div>
    <div class="rt-body">${d.runthrough}</div>
  </div>`;
}
```

---

## 还有的遗留问题

### 角度数据缺失（3支视频比对功能失效）

| 视频 | 角度覆盖 | 影响 |
|------|---------|------|
| 清明雨上 | 9/9 ✅ | 无 |
| 群舞·琵琶曲 | 5/8 🔶 | 部分 |
| 舞蹈1/2/3 | 0/9、0/8、0/6 ❌ | 比对功能不可用 |

**根因：** 这3支视频 pose estimation 没跑完就入库了。

**修复方式（需 SSH 进服务器）：**
```bash
ssh karen@47.242.80.65
cd /www/wujing-api
# 对每个缺角度的视频重跑 pose
python3 pose_angles.py data/vid_1679971642/frames/
python3 pose_angles.py data/vid_2130688639/frames/
python3 pose_angles.py data/vid_563626483/frames/
```

---

## Git 状态

```
branch:  auto-decompose-mediapipe-0730
latest:  e9da9e7  chore: 补充 patches/ 目录
前一个:  bdd5b31  feat: Claude 生成「过一遍剧本」+ 修口诀retry bug
clean:   ✅ 无未提交
```

---

## 评分总结

| 时间 | 评分 | 里程碑 |
|------|------|--------|
| 今早接手 | 6.2/10 | 遗留状态 |
| P0 完成 | 7.5/10 | 安全稳定 |
| P1 完成 | 8.0/10 | 体验达标 |
| 上午离手 | 8.8/10 | 生产级 |
| **下午离手** | **9.5/10** | **接近完美** |
| 加 runthrough 后 | 9.5+/10 | 新功能加分 |

*生成：2026-08-09 下午 · 下次接手先做：前端展示 runthrough + 服务器加 ANTHROPIC_API_KEY*
