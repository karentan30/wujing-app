# 舞镜 · 交接 0811

## 做了什么
- 15个P0/P1修复部署上线（之前只在git没上服务器·服务器非git不会自动pull）
- 修 `/app` 舞蹈卡黑柱bug（flex容器缺 `align-items:flex-start`，卡片被拉成全屏黑柱）
- 配色统一银色（`/app` 保持 `--bg:#f0f2f8`）
- **救回昨晚丢失的银色好首页**：`home-v2.html` 141KB版（含3张真实拆解卡+3张功能卡）从服务器同步回本地 `_live/`

## 🚨 红线坑（这次踩的）
- **服务器非git·scp部署**：直接在服务器上改的东西，本地/git/交接全没有，换会话就"丢"。昨晚00:14的银色好首页就是这样，今早差点被覆盖。
- **改动必须回写本地 `_live/`**：`/` → `static/home-v2.html`，`/app` → `static/design-upgrade.html`。改完服务器一定 scp 回本地或反之。
- 部署路径：`root@47.242.80.65:/www/wujing-api/static/`（不是pm2·不是/app/wujing·交接脚本路径是错的）

## 线上现状
- `/` 首页 = 银色3卡好版（《清别欢》baipai/jiyi/gushi.png · assets/ 目录）✅200
- `/app` 拆舞页 = 银色·黑柱已修 ✅200
- WJ_FREE_MODE=0（收费模式·¥9.9微信）

## 全目录对账结果（0811·堵"丢改动"）
- ✅一致(5)：design-upgrade / home-v2 / server.py / auto_decompose / library_index（`_live/`顶层=服务器镜像·`_live/static/`子目录是旧残留噪音忽略）
- 🆘服务器独有→已救回本地(3)：`auth-pay.html`(付款页) / `privacy.html` / `terms.html`（本地原本没有·差点丢）
- ⬆️已部署(2)：`class_teacher.html`(+学员筛选) / `class_join.html`(+维度tooltip·⚠️_esc丢了单引号转义待补)
- ⏸️**pay.py 未部署·碰钱待办**：本地(08-08)有支付安全加固(先鉴权再解析payload+JSON错误处理)·线上(08-05)是旧版没加固。**等Karen真机付款测通后·单独走"部署加固版+微信0.01真扫复测"**·别在验证付款前动支付代码。
- ✅烧钱门`/api/generate-bg`焊死：无token/假token→401·登录后每人每天20次(429)·实测通过。

## 待办
- **P0 真机测**：手机 `/app` → 上传 → 付¥9.9 → 验证拆解卡（只有Karen能做）
- **pay.py加固版部署**（Karen付款测通后·staging测+微信0.01真扫）
- class_join.html 的 `_esc` 补回单引号转义(`'`→`&#39;`)
- 部署脚本 `部署脚本-一键上线-20260810.sh` 路径是错的（pm2/app-wujing），要改成 scp+systemd
- 3张卡图目前是《清别欢》静态成品·后续可换真实用户拆解结果
