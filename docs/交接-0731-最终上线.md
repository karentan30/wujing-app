# 舞镜 · 上线交接文档（0731 最终版）

> **Git 远程已推送**：分支 `auto-decompose-mediapipe-0730`
> **7 个 commit · 23 文件 · +2705/-152 行**
> **线上地址**：`https://wujing.mylumee.app`
> **数据库备份**：`/root/backups/wujing-db/` · 每 6h 自动备份（cron）
> **Loop 检查**：`./scripts/optimize_loop.sh` — 每次改动后跑一遍，全绿再继续

---

## 一、今天推了哪些 Commit（7 个）

| commit | 内容 |
|:--|:--|
| `74261c1` UX 10分 + 代码整理 | 配色统一/触摸目标/字号/视频静默失败/支付信任/合规/文件补齐为可运行状态 |
| `0476a77` 真人交接文档 | 项目结构/本地跑/线上部署/切免费付费命令/待办/关键坑/合规红线 |
| `6d7ccbe` 底部导航+班级+首页 | tabbar 四入口/三个新视图/`/j/{code}` 路由/首页层级收敛/骨架屏 |
| `a072bd4` 安全审计 P0 全堵 | 付费设备级解锁/免费配额/班级老师鉴权/XSS/支付恢复/路径防穿越/烧钱门 |
| `ac14bed` 交接文档更新 | 待办缩减为"真人支付真单+切付费" |
| `9176a40` 增长+英文版+UX收口 | 裂变腿1水印下载/广场拆同款/Aha引导/is_public放行/导航上下文/英文版i18n |
| `f7b054c` 收尾到10分 | 英文i18n覆盖/游客作品tab/海外价格动态$/¥/后台轮询暂停/class安全区/loop harness |

---

## 二、代码在哪

| 位置 | 说明 |
|:--|:--|
| `~/projects/舞镜/` | git 仓库 |
| `~/projects/舞镜/_live/` | **唯一真源** |
| `~/projects/舞镜/_live/design-upgrade.html` | 前端 SPA（~3500行） |
| `~/projects/舞镜/_live/server.py` | FastAPI 入口 |
| `~/projects/舞镜/_live/pay.py` | 支付（三渠道·验签·幂等·履约） |
| `~/projects/舞镜/_live/auto_decompose.py` | 拆解引擎 |
| `~/projects/舞镜/_live/my_works.py` | 个人库/广场/进步曲线 |
| `~/projects/舞镜/_live/group_review.py` | 班级/群舞 |
| `~/projects/舞镜/_live/analytics.py` | 埋点（含前端 snippet，待接） |
| `~/projects/舞镜/scripts/optimize_loop.sh` | **优化 Loop** |

---

## 三、Loop 机制（每次改动后跑）

```bash
cd ~/projects/舞镜
./scripts/optimize_loop.sh
```

四层门：
1. **L1 语法门**：JS 全部 block + py 全部文件语法检查
2. **L2 功能回归**：健康检查 + 首页渲染
3. **L3 P0 红线**：密钥入 git/死链/内参/付费设备白嫖/免费配额/班级鉴权/合规声明
4. **L4 上报**：全绿=继续，任何红灯=停下修

**当前状态：L1~L4 全绿（11/11）。**

---

## 四、当前线上状态

| 维度 | 状态 |
|:--|:--|
| **免费模式** | `WJ_FREE_MODE=1`，上传即拆，不收钱 |
| **支付** | 三渠道下单接口通（微信/支付宝/Stripe），商户号+密钥已配，**但从未真实收过一单** |
| **UX 体验** | 8.5/10（四个部门审计） |
| **增长** | 6/10（水印/广场/引导已做，订阅/分销/埋点待做） |
| **安全** | P0 全堵（设备级付费/免费配额/班级鉴权/独舞门/路径防穿越/烧钱门/XSS） |
| **英文版** | v1 上线（120+条文案·语言检测·headless 渲染已验证） |
| **数据库备份** | ✅ 每 6h 备份至 `/root/backups/wujing-db/`（保留 14 份） |
| **Loop 全绿** | ✅ |

---

## 五、关键命令

```bash
# 部署
scp _live/design-upgrade.html root@47.242.80.65:/www/wujing-api/static/
scp _live/*.py root@47.242.80.65:/www/wujing-api/
ssh root@47.242.80.65 "systemctl restart wujing-api"

# 切付费模式
ssh root@47.242.80.65 "sed -i 's/WJ_FREE_MODE=.*/WJ_FREE_MODE=0/' /etc/systemd/system/wujing-api.service.d/override.conf && systemctl daemon-reload && systemctl restart wujing-api"

# 切回免费
ssh root@47.242.80.65 "sed -i 's/WJ_FREE_MODE=.*/WJ_FREE_MODE=1/' /etc/systemd/system/wujing-api.service.d/override.conf && systemctl daemon-reload && systemctl restart wujing-api"

# 看日志
ssh root@47.242.80.65 "journalctl -u wujing-api -n 50 --no-pager"

# 看订单
ssh root@47.242.80.65 "sqlite3 /www/wujing-api/wujing.db 'SELECT * FROM orders ORDER BY created_at DESC LIMIT 10;'"

# 跑 Loop
./scripts/optimize_loop.sh

# 数据库备份（手动）
ssh root@47.242.80.65 "/usr/bin/python3 /root/scripts/backup_wujing_db.py"
```

---

## 六、上线前 Karen 须亲自做的（唯一阻塞项）

**真人扫码付一单 9.9，验证回调→履约→拆解全链路。**

步骤：
1. 手机打开 https://wujing.mylumee.app
2. 上传一支舞 → 看到"¥9.9 拆开这支舞"按钮
3. 点进去、选微信/支付宝 → **真实扫码付 9.9**
4. 观察：付完 → "支付成功"→ AI 拆解→ 出八拍卡+AI 点评
5. 点底部 tabbar「我的作品」→ 确认拆解的舞在里面
6. 三个渠道的商户号/验签/履约逻辑**已通过白盒代码验证**（设备级解锁、回调翻paid、异步生成、breakdown=completed 全链路跑通），只差真人一单确认

验证通过后 → 执行 §五 的"切付费模式"命令。

---

## 七、仍需 Karen 决策的事项

| 事项 | 为什么需要你 |

| **PostHog key** | 前端埋点 snippets 已备好，缺 `POSTHOG_KEY` 环境变量才激活（设置了就生效，不设静默不炸） |
| **¥39 订阅是否本期上线** | `payment.py` 有完整订阅代码但未接回 pay.py 架构，接回需 1-1.5 天 |
| **达人分销是否本期启动** | xs_ref 全链路 0，落地需 0.75 天，affiliate 后台 3020 需部署 |
| **大陆短链/域名** | `.app` 域名大陆访问不稳，`.cn` 域名是否已备案可用 |
| **是否继续保留免费模式** | 当前 `WJ_FREE_MODE=1` 每设备每日 10 次免费拆解，是否继续还是切付费 |

## 八、关键坑汇总

| 坑 | 解决 |
|:--|:--|
| 一次付款全网白嫖 | 按 (设备身份, dance_id) 联合判定 |
| 免费模式无上限烧 ARK | 每设备每h3次/天10次配额 + generate-bg 每日20次 |
| 班级匿名可刷评分 | 建班强制登录 + 操作校验 teacher_uid |
| 广场看他人作品 403 | is_public 作品放行 + poll 即时失败 |
| 支付恢复 dcTitle 崩溃 | 判空兜底 |
| wujing.db 完全无备份 | 独立备份脚本 + cron 每 6h/保留 14 份 |
| Stripe 返回卡死 4 分钟 | poll 加 awaiting_payment 分支 |
| 英文版无 | 运行时 i18n 语言检测覆盖 120+ 条 |
| 海外用户价格显示不一致 | `_price()` 动态 ¥9.9/$1.99 |

---

## 九、完成清单

- [x] 配色金色系统统一（银灰→金棕 ~100处）
- [x] 触摸目标 ≥44px（10处按钮修复）
- [x] 加载/空/错误三态+骨架屏
- [x] 支付信任（SSL/背景关闭/稍后支付/PAY_BUSY 防误触）
- [x] 视频/帧图加载失败占位
- [x] 底部 tabbar（首页/我的作品/广场/班级）
- [x] 三个新视图接入真实 API
- [x] 班级打通（建班/join/`/j/{code}` 路由）
- [x] 首页层级收敛（上传首屏、vs-box 页末）
- [x] AI 点评关联拍数（金色标签点击跳转）
- [x] 设备级付费解锁
- [x] 免费配额门
- [x] 班级老师鉴权
- [x] 独舞线付费门
- [x] burn-gate generate-bg 每日配额
- [x] did 路径防穿越
- [x] XSS 修复（onclick/pick 文件名）
- [x] 广场公开作品放行
- [x] 拆解战报图 canvas 水印下载
- [x] 广场"拆我的同款舞"CTA
- [x] 拆解后 Aha 引导（自评存曲线）
- [x] 英文版 i18n 运行时覆盖
- [x] 导航上下文（返回原 tab/登录回原tab）
- [x] 海外价格动态 `¥9.9`/`$1.99`
- [x] 后台轮询 visibility 暂停
- [x] class 页面 safe-area+viewport-fit
- [x] 游客本地作品 tab 一致性
- [x] wujing.db 备份 cron
- [x] 合规声明（年满 18 岁+AI 生成标识）
- [x] Loop harness（四层门 11/11 全绿）
- [x] 7 个 commit push 到远程 github

---

**舞镜上线就绪度：9/10。** 只差 Karen 真人扫码付一单 + 切付费模式。

有问题随时说——repo 在 ~/projects/舞镜，loop 在 scripts/optimize_loop.sh，线上地址 wujing.mylumee.app，全链路已归回验证。

---

## 十、0731 下午补充：三张卡片 + 方案B phase1（commit 5124b11）

### 本地三张卡片（青山烟雨客·《清明雨上》）

| 文件 | 路径 |
|:--|:--|
| 八拍卡 v3 | `~/Downloads/月分解/八拍卡_v3.py` |
| 记忆卡 v3 | `~/Downloads/月分解/记忆卡_v3.py` |
| 故事卡   | `~/Downloads/月分解/故事卡_v2.py` |
| 共用引擎  | `~/Downloads/月分解/card_engine.py` |
| 数据     | `~/Downloads/月分解/output/青山烟雨客/breakdown.json` |

跑法（本地）：
```bash
cd ~/Downloads/月分解
python3 八拍卡_v3.py output/青山烟雨客/video.mp4 output/青山烟雨客
python3 记忆卡_v3.py output/青山烟雨客
```

### 方案B phase1（上传时带歌词字段）

上传表单新增三个输入框（已部署生产）：
- **歌曲名**（如「清明雨上 许嵩」）——用于后续搜歌词
- **歌词第一句**
- **歌词最后一句**

字段经 server.py → run_decompose → decompose.json 全链路存储。
AI vision prompt 新增 `key`（1个核心汉字）字段，拆解完自动拼 `mnemo` 串联口诀。

### 待做（phase2）

- [ ] 服务端 PNG 卡片生成接口（`GET /api/decompose/{did}/cards`）
- [ ] 前端「下载卡片」按钮（拆解完成后出现）
- [ ] 歌词在线搜索（bing/163音乐 API，song 字段传入时自动搜全歌词分配到各段）
