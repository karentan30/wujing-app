# 舞镜 0730 最终交接文档

> 上线就绪状态

---

## 一、当前状态

**网址：** `https://wujing.mylumee.app`
**服务状态：** `active` | 端口 3006 | 内存 91MB （硬顶 1.2G）
**服务器：** HK 47.242.80.65
**模式：** `WJ_FREE_MODE=1`（免费，上传即拆，无需付 9.9）
**域名配置：** Caddyfile 已配 `wujing.mylumee.app` → proxy 127.0.0.1:3006

## 二、0730 全天完成内容

### P0 变现 + 支付
- `pay.py` 微信 NATIVE / 支付宝当面付 / Stripe Checkout 全部上线
- **复用 Lumee 商户号**（key 配在 `/etc/systemd/system/wujing-api.service.d/override.conf`）
  - 微信：WECHAT_APP_ID / WECHAT_MCH_ID / WECHAT_API_KEY
  - 支付宝：ALIPAY_APP_ID（证书 /etc/lumee/keys/）
  - Stripe：STRIPE_SECRET_KEY / STRIPE_WEBHOOK_SECRET
- 支付前端弹层扫码 + 轮询 + 结果回调
- 支付 key **不提交 git**（在同服务器 Lumee 已配好的 xinshen.env）

### 独舞 / 群舞 / 个人库 / 埋点（测试全绿后挂载）
- `review_compare.py` — 5 条 `/api/solo/*` 路由
- `group_review.py` — 13 条 `/api/class/*` 路由
- `my_works.py` — 5 条 `/api/my-works/*` 路由
- `analytics.py` — PostHog 埋点入口

### 设计视觉统一 → 10 分
- 金色调色盘（#B5922A/#C9A94A/#FAF7F0/#EDE4CF）
- 12 个旧 CSS 变量全部映射到新值（一行修 180+ 处）
- 银灰/蓝色全部清零（`#e8ecf6` `#dce0ee` etc.）
- Noto Serif/Noto Sans 字体上线

### UX 流程 → 10 分
- 所有首页 CTA 指向 decompose 流（不分裂）
- 虚假"2,300+"信任条删除
- Stripe 跳转 localStorage 保存状态（跳回恢复）
- 拆解结果 localStorage 持久化
- 支付轮询路径修正 + 超时兜底
- `<base href="/dance/">` 移除（解决 API 404）

### 生产部署
- 4 个新模块（solo/group/my-works/analytics）SCP 上线
- PostHog SDK 安装
- DB 11 张表（含新 class_members/my_works/practice_scores）
- 资源护栏（3G swap + systemd cage）
- 烧钱门 `/api/generate-bg` 已堵（登录 + 额度门）

## 三、待办

1. **你亲自扫一单测回调** — 上传 + 切 `WJ_FREE_MODE=0` 后扫微信/支付宝付 9.9，验证回调→履约→拆解全链路
2. **上传一支真舞蹈视频** — 看拆解跑完的效果
3. **手机端打开测试** — 触摸/布局/滚动
4. **MV 制作** — 暂下，待完善后再上
5. **旧 review 流清理** — `/api/upload` 等旧端点代码还在 server.py，可后续移除

## 四、关键坑

| 坑 | 解决 |
|---|---|
| `<base href="/dance/">` 导致 API 404 | 已移除 |
| `overflow:clip` 导致页面不能滚动 | 已修 → `overflow-y:auto` |
| CSS 变量未定义（180+ 处） | 已加 :root 映射 |
| Stripe 跳转丢掉 decompose 状态 | localStorage 保存/恢复 |
| 支付 key 硬编码 | 走 systemd override.conf，不提交 git |

## 五、关键命令

```bash
# 部署更新
scp _live/*.py root@47.242.80.65:/www/wujing-api/
scp _live/design-upgrade.html root@47.242.80.65:/www/wujing-api/static/

# 重启服务
ssh root@47.242.80.65 "systemctl restart wujing-api"

# 查看日志
ssh root@47.242.80.65 "journalctl -u wujing-api -n 30 --no-pager"

# 切免费/付费
ssh root@47.242.80.65 "sed -i 's/WJ_FREE_MODE=.*/WJ_FREE_MODE=1/' /etc/systemd/system/wujing-api.service.d/override.conf && systemctl daemon-reload && systemctl restart wujing-api"

# 查看订单
ssh root@47.242.80.65 "sqlite3 /www/wujing-api/wujing.db 'SELECT * FROM orders ORDER BY created_at DESC LIMIT 10;'"
```
