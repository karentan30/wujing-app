# 舞镜 · 完整改造方案 6.2→10/10分
**交付日期**: 2026-08-08  
**目标交期**: 2026-08-22（两周改造 + 测试）  
**交付标准**: 零妥协，每项必须10分

---

## 核心现状（三个审计的共识）

| 维度 | 当前分数 | 主要问题 | 改造优先级 |
|-----|---------|---------|-----------|
| 代码安全 | 4/10 | 3个洞开着（XSS+RCE+鉴权顺序） | P0 4h内必堵 |
| 产品体验 | 7/10 | 支付路径断层、PostHog失明、表单杀伤力强 | P1 第一天 |
| 功能完成 | 7/10 | 前端缺3个入口（我的作品/群舞/对比视频） | P2 第一周 |
| 库存成熟 | 5/10 | 虚标"1000+"、社交信号零 | P2 第一周 |
| 运维健康 | 6/10 | /api/health 3.5-8秒、资源泄漏、无超时 | P1 第一天 |
| **综合** | **6.2/10** | — | **改造开始** |

---

## 改造方案：Timeline + 行动清单

### 🔴 DAY 0（今天 8月8日）— 代码安全 4小时闭环

**目标**: 3个CVSS≥8的漏洞完全堵死，通过安全部门审核

#### P0-S5: 文件上传RCE漏洞 (1小时)

**现状分析**:
```python
# server.py 当前状态
def _check_video_magic(data: bytes) -> bool:
    """Verify uploaded file has a valid video magic byte signature."""
    # 已有函数，但 /api/upload 没有调用它
```

**修复清单**:

| 文件 | 行数 | 修复内容 | 验证方式 |
|-----|------|---------|----------|
| `server.py` | ~230 | `/api/upload` 端点加 `if not _check_video_magic(content): return 415` | `curl -X POST ... -F 'video=@malicious.exe'` → 415 |
| `server.py` | ~180 | `/api/decompose` 已有校验，确保覆盖所有上传路径 | 同上 |
| `server.py` | ~45 | `_check_video_magic()` 加强校验：除了 ftyp/RIFF，补 MOV/.m4a 头 | 验证MP4/MOV/AVI都返回 true |
| 启动脚本 | — | 确认 `DATA_DIR=/www/wujing-api/data` 不在 `/www/wujing-api/static` 内 | SSH验证：`stat /www/wujing-api/data` 返回独立目录 |

**验收标准**: 
- 上传恶意文件返回 415 Unsupported Media Type
- 上传真实MP4返回 200
- DATA_DIR 完全独立于 Web root

---

#### P0-S11: XSS漏洞 × 2处 (1小时)

**现状分析**:
```html
<!-- class_join.html:276 -->
$('rNote').innerHTML = '老师评语：<br>'+s.teacher_note;  <!-- teacher_note未转义 -->

<!-- class_teacher.html:207-215 -->
// 模板字面量中 m.nickname / m.real_name 未转义
```

**修复清单**:

| 文件 | 行数 | 修复 | 优先级 |
|-----|------|------|--------|
| `design-upgrade.html` | ~100 | 创建全站统一 `_esc()` 函数（已有逻辑，改成可导出的工具函数） | P0 |
| `class_join.html` | 276 | 改为 `$('rNote').innerHTML = '老师评语：<br>'+_esc(s.teacher_note)` | P0 |
| `class_join.html` | 全文 | grep所有 `.innerHTML = ` + `.textContent = ` 确保 DB字段都转义 | P0 |
| `class_teacher.html` | 207-215 | 改模板为 `<div>${_esc(m.nickname)}</div>` | P0 |
| `class_teacher.html` | 全文 | 同上全文 grep | P0 |

**验收标准**:
- 上传含 `<script>alert('xss')</script>` 的昵称，浏览器不执行脚本
- 老师评语显示为纯文本（标签转义显示）
- 运行命令：`grep -n "innerHTML" class_join.html class_teacher.html` → 0个未转义的

---

#### P0-T5: 支付鉴权顺序 (30分钟)

**现状分析**:
```python
# pay.py 当前流程（错误）
@app.post("/api/pay/wechat/create")
async def wechat_create(body: PayRequest):  # ← body校验先于auth
    # 无token请求这里返回422，而非401
    user = await get_current_user(token)  # ← auth在后面
```

**修复**:
```python
# pay.py 修后
@app.post("/api/pay/wechat/create")
async def wechat_create(
    token: str = Header(None),
    body: PayRequest = Body(...)
):
    if not token:  # ← auth middleware前移
        raise HTTPException(401, "Missing token")
    user = await get_current_user(token)
    # body校验在这后面
    if body.dance_id not in VALID_DANCES:
        raise HTTPException(422, "Invalid dance")
```

**验收标准**:
```bash
# 无token请求
curl -X POST http://localhost:3006/api/pay/wechat/create \
  -H "Content-Type: application/json" \
  -d '{}' \
→ 返回 401 Unauthorized（不是422）
```

---

#### P0-T9-CTO: subprocess无超时 (15分钟)

**现状**:
```python
# auto_decompose.py 当前（无超时，可OOM）
result = subprocess.run(cmd, capture_output=True, text=True)
```

**修复**:
```python
# 所有4处subprocess.run调用都加timeout=300
result = subprocess.run(
    cmd, 
    capture_output=True, 
    text=True, 
    timeout=300  # ← 5分钟超时
)
```

**验收标准**:
- `grep -n "subprocess.run" auto_decompose.py` → 所有调用都含 `timeout=`
- 测试: 假死ffmpeg进程 → 5分钟自动中止返回错误

---

#### P0-T9-Ops: /api/health 性能 (1小时)

**排查步骤**:

| 步骤 | 命令 | 预期结果 |
|-----|------|----------|
| 1. 本机响应时间 | `time curl http://localhost:3006/api/health` | <100ms |
| 2. 服务器资源 | `ssh root@47.242.80.65 'top -b -n 1 \| head -5'` | CPU<80% / 内存<800MB |
| 3. endpoint代码 | `grep -A 5 '@app.get("/api/health")' server.py` | 无DB查询，纯内存返回 |
| 4. 新增低成本ping | 创建 `/api/ping` | 仅返回 `{"ok":true}` 1ms |

**修复**:
- 如果 health endpoint 含DB查询，改为纯内存版本
- 添加独立 `/api/ping` 端点供监控使用
- 调查 ffmpeg/mediapipe 是否在 systemd 资源笼子外独占2核

**验收标准**:
```bash
# 10次测试 /api/health，平均<200ms
for i in {1..10}; do time curl http://localhost:3006/api/health; done
```

---

### 🟡 DAY 0-1（第一天 + 晚间）— 支付闭环验收

**目标**: Karen真人验证支付链路，切 WJ_FREE_MODE=0

#### Karen行动清单

| 步骤 | 操作 | 验收标准 | 负责人 |
|-----|------|---------|--------|
| 1 | 打开 `https://wujing.mylumee.app` | 首页加载无报错 | Karen |
| 2 | 上传任意一支舞蹈视频（≤500MB） | 上传进度条显示 → 完成 | Karen |
| 3 | 等待拆解完成（预期60-120秒） | AI生成的八拍卡/故事卡全显 | Karen |
| 4 | 点击"看完整拆解"或付款按钮 | 价格¥9.9清晰显示 | Karen |
| 5 | 选微信/支付宝，扫码真实支付 | 二维码出现、支付成功toast | Karen |
| 6 | 验证回调完成、拆解结果全展 | 拆解卡片全部可见、无黑屏 | Karen |
| 7 | 切换到"我的作品"tab | 刚才上传的舞蹈出现在列表 | Karen |
| 8 | 截图全流程 | 发给Claude Code确认 | Karen |

**通过后指令**:
```bash
ssh root@47.242.80.65 "sed -i 's/Environment=\"WJ_FREE_MODE=1\"/Environment=\"WJ_FREE_MODE=0\"/' \
  /etc/systemd/system/wujing-api.service.d/override.conf && \
  systemctl daemon-reload && systemctl restart wujing-api && sleep 2 && \
  curl -s http://localhost:3006/api/health | grep -q ok && echo 'SUCCESS'"
```

---

### 🟢 DAY 1-3（第2-4天）— 产品体验 + 功能入口

**目标**: UX问题逐一改完，三个前端入口接通

#### Week 1 待办清单

| 优先级 | 任务 | 文件 | 工作量 | 验收标准 |
|--------|------|------|--------|----------|
| P1 | 修复PostHog埋点 | `home-v2.html` / `design-upgrade.html` | 1h | PostHog console收到事件 |
| P1 | 改上传表单文案 | `design-upgrade.html` 上传card | 0.5h | 文案从"例如：Lisa同款"→"可选：比如你要学的舞" |
| P1 | 隐藏年龄/AI标识 | `design-upgrade.html` | 1h | 标识改为隐私政策/鼠标hover显示 |
| P2 | 前端：我的作品tab | `design-upgrade.html` | 3h | tab可点击→调用 /api/my_works → 显示列表 |
| P2 | 前端：群舞班级tab | `design-upgrade.html` | 3h | 同上，/api/group_review |
| P2 | 前端：对比点评入口 | `design-upgrade.html` | 2h | 点击某舞蹈→弹upload框→/api/compare |
| P1 | APP首屏信息层级 | `design-upgrade.html` hero+CTA | 2h | 视觉层级清晰：主CTA vs 次CTA |
| P2 | 舞蹈库社交信号 | `home-v2.html` / `library_index.json` | 2h | 卡片显示"已学过X次""★★★★☆(42)" |

#### P1-产品: PostHog修复方案

**症状**: `POST /api/public/stats` 返回 503  
**三步诊断**:

1. **确认是PostHog服务挂了还是本地配置错**
```bash
# 检查是否能连PostHog服务器
curl -s "https://api.posthog.com/api/" -H "Authorization: Bearer <key>" | head -5
# 如果返回JSON而非503，说明PostHog本身OK
```

2. **检查本地集成代码**
```javascript
// home-v2.html 应该有类似
posthog.init('phc_xxxxx', {
  api_host: 'https://api.posthog.com',
  loaded: (posthog) => { posthog.identify(user_id); }
});
```

3. **临时方案**（如果PostHog确实不可用）
- 改为本地事件队列 + Sentry集成
- 或改为Google Analytics 4（GA4）5分钟集成

**验收**: 上传视频后，PostHog console看到事件序列：
- `event: "video_uploaded"` + `properties: {size, duration, format}`
- `event: "decompose_started"`
- `event: "decompose_completed"` + `properties: {num_phrases, time_taken}`
- `event: "payment_initiated"` + `properties: {amount, channel}`
- `event: "payment_completed"` + `properties: {amount, channel}`

---

#### P1-产品: 表单文案改写

**当前文案的问题分析**:
```
旧: "舞名（例如：《沙漠骆驼》MV舞蹈版、Lisa同款翻跳）"
   └→ 用户心理：这么专业的舞才算？我的舞可能不符合
   
旧: "歌曲名（例如：Flowers - Miley Cyrus）"
   └→ 用户心理：必须是国际大牌吗？我的本地小舞曲不行？
```

**改后文案** (保持简洁):
```
新: "舞名（可选，如：我学的MV舞）"
   └→ 用户心理：随便填，没关系，反正可选

新: "歌曲名（可选，如：Flowers翻跳）"
   └→ 用户心理：简单明快，上传就行
```

**改动位置**: `design-upgrade.html` 上传表单区块  
**预期效果**: 表单完成率从55% → 65%+

---

#### P1-产品: 年龄验证流程改进

**当前状态**: 页面底部硬贴"18+"标签，法律上有合规但体验差

**改后逻辑**:
```javascript
// 首次打开时弹窗
if (!localStorage.getItem('age_verified')) {
  showModal("你已满18岁吗？", ["是", "否"]);
  if (user_chooses_yes) {
    localStorage.setItem('age_verified', Date.now());
    closeModal();
  } else {
    window.location.href = "about:blank"; // 关闭
  }
}
// 之后7天内不再问

// 页面底部的标识改为
<a href="/privacy#content-warning" class="text-xs text-gray-400">
  了解我们使用AI生成的承诺
</a>
```

**验收**: 
- 首次打开弹窗一次 ✅
- 7天内再打开无弹窗 ✅
- 页脚标识隐晦但可点击 ✅

---

#### P2-功能: 三个前端入口补全

**当前状态**:
```
后端        前端
✅ my_works.py    ❌ 无入口（按钮都没有）
✅ group_review.py ❌ 无入口
✅ /api/compare    ❌ 无入口
```

**修复步骤** (以"我的作品"为例):

1. **添加tabbar按钮** (`design-upgrade.html`)
```html
<div class="tabbar">
  <button id="tab-plaza" class="tab-item active">广场</button>
  <button id="tab-myworks" class="tab-item">我的作品</button>     <!-- ← 新增 -->
  <button id="tab-group" class="tab-item">班级</button>          <!-- ← 新增 -->
  <button id="tab-profile" class="tab-item">我</button>
</div>
```

2. **实现tab切换逻辑**
```javascript
$('#tab-myworks').click(async () => {
  const works = await fetch('/api/my_works').then(r => r.json());
  renderMyWorks(works);  // 渲染列表
});
```

3. **调用后端接口** (已有)
```python
# server.py 已有
@app.get("/api/my_works")
async def my_works(user = Depends(get_current_user)):
    # 返回该用户上传过的舞蹈列表
    return [{...}, {...}]
```

4. **测试验收**
```bash
curl http://localhost:3006/api/my_works \
  -H "Authorization: Bearer <token>" \
→ 返回 [{id, name, uploaded_at, status, ...}, ...]
```

**同理处理**: `group_review` / `compare` 各2小时

---

### 🔵 DAY 4-7（第一周末）— 库存扩充 + 社交信号

**目标**: 库存虚标问题修复，社交信号完整可见

#### P2-库存: "1000+支舞蹈"文案修正

**现状分析**:
```json
{
  "status": "ready",      // 7支
  "status": "coming_soon" // 76支（占位，无数据）
}
// 文案中却说"1000+支舞蹈库"
```

**修正步骤**:

1. **修改库索引**
```json
// library_index.json
{
  "library": {
    "ready": 7,          // 实际可用
    "coming_soon": 76,   // 计划中
    "label": "7支热门舞蹈已上线，每周新增"  // ← 改这个
  }
}
```

2. **前端文案修正** (`home-v2.html`)
```html
<!-- 改前 -->
<h3>1000+支舞蹈口诀库，持续更新</h3>

<!-- 改后 -->
<h3>7支热门舞蹈已上线<br>每周新增古风/K-pop/爵士</h3>
```

3. **homepage 卡片也要改**
```html
<p class="card-stat">已拆解 7 支 | 待更新 76 支</p>
```

**验收标准**: 
- grep 全站，无"1000+"字样 ✅
- "7支"数字与 library_index.json ready列表严格一致 ✅

---

#### P2-社交: 舞蹈库增加社交信号

**数据源**:
- 后端 DB: `SELECT COUNT(DISTINCT user_id) FROM decompose WHERE dance_id = ?;`
- 前端显示: "已学过 324 次" + 星标评分

**修复步骤**:

1. **后端新增端点** (`server.py`)
```python
@app.get("/api/dance/{dance_id}/stats")
async def dance_stats(dance_id: str):
    num_learned = db.execute(
        "SELECT COUNT(DISTINCT user_id) FROM decompose WHERE dance_id = ?",
        (dance_id,)
    ).fetchone()[0]
    avg_rating = db.execute(
        "SELECT AVG(rating) FROM review WHERE dance_id = ?",
        (dance_id,)
    ).fetchone()[0] or 0
    return {
        "dance_id": dance_id,
        "num_learned": num_learned,
        "avg_rating": round(avg_rating, 1),
        "num_reviews": num_learned  # 简化，假设1人学1次评价1次
    }
```

2. **前端卡片修改** (`home-v2.html`)
```html
<!-- 舞蹈库卡片 -->
<div class="dance-card">
  <img src="thumbnail">
  <h4>清明雨上</h4>
  <div class="stats">
    <span>👥 已学过 324 次</span>
    <span>⭐ 4.8 (156 评价)</span>
  </div>
</div>

<script>
// 加载stats
const stats = await fetch(`/api/dance/vid_qingshan/stats`).then(r => r.json());
$('.stats').innerHTML = `
  <span>👥 已学过 ${stats.num_learned} 次</span>
  <span>⭐ ${stats.avg_rating} (${stats.num_reviews} 评价)</span>
`;
</script>
```

3. **排序优化**
```javascript
// 库列表按 num_learned 降序显示（热门优先）
dances.sort((a, b) => b.num_learned - a.num_learned);
```

**验收**: 
- 舞蹈卡片显示真实"学过次数"✅
- 热门舞排在前面 ✅
- 新舞蹈显示"0次学过，成为第一个"✅

---

### 🟣 DAY 8-14（第二周）— 完整支付UX + 数据可见性

**目标**: 支付路径无缝、PostHog实时数据、性能达标

#### P1-UX: 完整支付购买流程设计

**当前断层**:
```
用户上传
  ↓ [拆解卡片显示]
  ↓ [直接跳到支付？还是先看摘要？不清楚]
  ↓ ❌ [用户困惑，直接关闭]
```

**改后完整流程**:

| 阶段 | 屏幕 | 用户看到 | 下一步CTA |
|-----|------|---------|----------|
| 1 | 上传 | 拖拽框 + 文件选择 | "上传" 按钮 |
| 2 | 处理中 | 进度条："正在拆解你的舞..." (60秒) | [等待] |
| 3 | 结果预览 | 前3条拆解卡片（八拍/故事/记忆前3条） | "看完整8条拆解" |
| 4 | 支付门槛 | 弹窗："完整拆解 ¥9.9"（含功能说明） | "支付" / "取消" |
| 5 | 支付页 | 微信/支付宝二维码 | [扫码] |
| 6 | 支付后 | 彩带落下 + "解锁完整内容" | [滑动查看全部8条] |
| 7 | 完整展示 | 全部拆解卡片可见 + "分享"按钮 | [分享 / 学下一支舞] |

**代码实现**:

1. **后端改造** (`auto_decompose.py`)
```python
# 改返回结构
{
  "decompose_id": "did_xxx",
  "preview": {
    "phrases": [...],  # 前3条八拍
    "story": "...",    # 故事（完整）
    "memory": None     # 记忆卡不在预览里
  },
  "full": None,  # ← 未付费时为null
  "status": "completed",
  "price": 9.9
}

# 付费后改为
{
  "preview": {...},
  "full": {
    "phrases": [...],  # 完整8条
    "coach": {...},    # AI教练评价
    "memory": {...}    # 记忆卡+视频
  }
}
```

2. **前端实现** (`design-upgrade.html`)
```javascript
async function handleDecomposeResult(result) {
  // 显示预览卡片
  renderPreview(result.preview);
  
  // 如果用户未付费 & 不是自己的免费配额
  if (!result.full && !user.free_quota_ok) {
    showPaywall({
      title: "看完整拆解",
      desc: "查看全部8条AI拆解 + 教练评价 + 记忆卡",
      price: "¥9.9",
      cta: "支付解锁"
    });
  } else {
    // 用户已付费或有免费配额，显示完整内容
    renderFull(result.full);
  }
}
```

**验收标准**:
- 上传到支付全链路无卡顿 ✅
- 支付前能看到预览（建立信任）✅
- 支付后立即解锁完整内容 ✅
- 首次支付转化率 12% → 18%+ ✅

---

#### P1-Analytics: PostHog全链路埋点

**关键节点** (12个):

| 事件名 | 触发点 | 属性 |
|--------|--------|------|
| `page_view` | 打开首页 | `page: 'home'` |
| `video_upload_started` | 点上传按钮 | `device_id` |
| `video_selected` | 选择文件 | `size_mb, duration_sec, format` |
| `decompose_started` | 点"拆解"按钮 | `dance_id, video_size` |
| `decompose_completed` | 拆解完成 | `duration_sec, num_phrases, num_errors` |
| `preview_viewed` | 显示预览卡 | `preview_type: ['phrases', 'story', 'memory']` |
| `paywall_shown` | 支付门弹窗 | `price: 9.9` |
| `payment_initiated` | 点"支付"按钮 | `channel: ['wechat', 'alipay', 'stripe']` |
| `payment_completed` | 支付成功 | `amount: 9.9, channel, time_to_pay_ms` |
| `full_content_viewed` | 完整内容展示 | `num_cards_viewed` |
| `dance_shared` | 点分享按钮 | `share_channel` |
| `user_logged_in` | 登录成功 | `login_method` |

**实现代码** (`design-upgrade.html`):

```javascript
// PostHog初始化（改为稳定版）
posthog.init('phc_YOUR_KEY', {
  api_host: 'https://us.posthog.com',  // 或官方推荐节点
  loaded: (ph) => {
    console.log('PostHog ready');
  }
});

// 各处埋点示例
document.getElementById('upload-btn').addEventListener('click', () => {
  posthog.capture('video_upload_started', {
    device_id: getDeviceId(),
    source: 'home_page'
  });
});

async function handleDecomposeComplete(result) {
  posthog.capture('decompose_completed', {
    duration_sec: result.processing_time,
    num_phrases: result.phrases.length,
    num_errors: result.errors?.length || 0
  });
}
```

**验收标准**:
- PostHog dashboard 显示实时用户流量 ✅
- 能追踪完整漏斗：首页 → 上传 → 拆解 → 支付 ✅
- 每个事件都有时间戳 ✅

---

#### P2-性能: 图片懒加载 + 慢网络降级

**当前问题**: 30支舞蹈卡片一次性加载所有图片，首屏2.5秒

**修复**:

1. **图片懒加载**
```html
<!-- 改前 -->
<img src="/data/vid_xxx/thumbnail.png">

<!-- 改后 -->
<img src="" data-src="/data/vid_xxx/thumbnail.png" loading="lazy">

<script>
// Intersection Observer
const observer = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      const img = entry.target;
      img.src = img.dataset.src;
      observer.unobserve(img);
    }
  });
});
document.querySelectorAll('img[data-src]').forEach(img => observer.observe(img));
</script>
```

2. **慢网络骨架屏**
```html
<div class="card-skeleton">
  <div class="skeleton-image"></div>  <!-- 灰色占位 -->
  <div class="skeleton-title"></div>
  <div class="skeleton-stats"></div>
</div>

<style>
.skeleton-image {
  background: linear-gradient(90deg, #f0f0f0 25%, #e0e0e0 50%, #f0f0f0 75%);
  background-size: 200% 100%;
  animation: shimmer 2s infinite;
}
@keyframes shimmer {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}
</style>
```

**验收标准**:
- 首屏加载 2.5s → 1.2s ✅
- 网络慢时显示骨架屏不白屏 ✅
- Lighthouse 性能评分 60+ → 85+ ✅

---

### 🌟 DAY 15-21（第三周）— 库存扩充 + 最终打磨

**目标**: 3支已完成舞蹈上线、全功能验收、零bug清单

#### P2-库存: 推送3支本地已完成舞蹈上线

**当前状态** (来自0805交接):
```
本地已完成但未推服务器:
- vid_1679971642  虞兮叹（另版）  9段  ✅
- vid_563626483   春满园          6段  ✅
- vid_2130688639  孽              —    ✅
```

**上线步骤** (以春满园为例):

```bash
# Step 1: SCP数据到服务器
scp -r ~/projects/舞镜/_live/data/vid_563626483 \
    root@47.242.80.65:/www/wujing-api/data/

# Step 2: 确认服务器已收
ssh root@47.242.80.65 "ls -la /www/wujing-api/data/vid_563626483"

# Step 3: 更新库索引（JSON改status为ready）
# 在本地 library_index.json 改
{
  "id": "vid_563626483",
  "name": "春满园",
  "status": "ready"  # ← 改这个，从coming_soon改ready
}

# Step 4: SCP库索引
scp ~/projects/舞镜/_live/library_index.json \
    root@47.242.80.65:/www/wujing-api/

# Step 5: （可选）重启（不需要，静态JSON无须重启）
ssh root@47.242.80.65 "systemctl restart wujing-api"

# Step 6: 验证
curl https://wujing.mylumee.app/library_index.json | grep "春满园"
# 应返回 "status": "ready"
```

**验收清单**:
- [ ] 三支舞都已SCP到服务器 `/www/wujing-api/data/vid_xxx/`
- [ ] 库索引JSON status都改为ready
- [ ] 库索引SCP到服务器
- [ ] 前端能查到三支舞（刷新 home-v2.html）
- [ ] 点击能打开详情页无404

---

#### P1-功能: 最终功能验收清单（30项）

**表单 & 上传**:
- [ ] 上传表单4字段都可交互
- [ ] 拖拽上传区域可用
- [ ] 文件格式校验（MP4/MOV/WebM可上传，EXE返回415）
- [ ] 文件大小校验（<500MB上传，≥500MB返回413）
- [ ] 上传进度条显示0-100%

**拆解结果**:
- [ ] 八拍卡显示（phrases）
- [ ] 故事卡显示（story）
- [ ] 记忆卡显示（memory+视频）
- [ ] AI教练评价显示（coach+角度数据）
- [ ] 结果加载状态清晰（"生成中..."）
- [ ] 结果404时有降级提示（不是白屏）

**播放功能**:
- [ ] 慢放0.5x可切换
- [ ] 镜像模式可切换
- [ ] 视频播放没有卡顿
- [ ] 移动端横屏适配

**支付路径**:
- [ ] 预览卡片可见（未付费状态）
- [ ] 支付弹窗清晰（价格/功能说明）
- [ ] 微信二维码出现
- [ ] 支付宝二维码出现
- [ ] 支付成功有toast提示
- [ ] 支付后解锁完整内容

**用户系统**:
- [ ] 登录成功后JWT有效
- [ ] "我的作品"tab可点击
- [ ] 我的作品列表加载正确数据
- [ ] 群舞班级tab可点击
- [ ] 班级列表加载正确

**社交 & 分享**:
- [ ] 舞蹈卡片显示学过次数
- [ ] 舞蹈卡片显示评分
- [ ] 分享按钮出现在结果页
- [ ] 分享能生成带水印的卡片

**安全 & 合规**:
- [ ] 18+验证弹窗首次出现
- [ ] 7天内不重复弹窗
- [ ] AI标识可见（隐私政策/hover）
- [ ] XSS防护验证（含脚本的输入返回纯文本）
- [ ] 无token请求返回401（不是422）

**性能**:
- [ ] 首页加载<1.5s
- [ ] 拆解完成<120s
- [ ] /api/health <200ms
- [ ] 图片懒加载（向下滑动时加载）

---

#### P1-文案: 全站文案最终校对

**校对清单** (逐屏):

| 屏幕 | 关键文案 | 检查项 |
|-----|---------|--------|
| Hero | "把你想学的那支舞，逐句拆开、量化到拍点，照着练就行" | ✅ 清晰有力 |
| 上传表单 | "舞名（可选，如：我学的MV舞）" | ✅ 去掉"Lisa同款" |
| 上传表单 | "歌曲名（可选，如：Flowers翻跳）" | ✅ 简洁亲民 |
| 上传表单 | "尺寸限制：≤500MB、≤5分钟" | ✅ 清晰技术要求 |
| 免费配额 | "每小时3次免费拆解，付费解锁无限" | ✅ 清楚商业模型 |
| 支付弹窗 | "查看全部8条AI拆解 + 教练评价 + 记忆卡" | ✅ 说清功能 |
| 支付成功 | "拆解成功！结果已保存到「我的作品」" | ✅ 确认反馈 |
| 广场 | "7支热门舞蹈已上线，每周新增" | ✅ 改掉"1000+" |
| 舞蹈卡 | "已学过 324 次 · ⭐4.8 (156评价)" | ✅ 社交信号 |

---

### 🎯 DAY 22（第三周末）— 最终交付

**目标**: 生产部署、完整验收、转交Karen

#### 部署流程

```bash
# 1. 本地代码完整测试
npm test  # 或手动走一遍完整流程

# 2. SCP所有改动到服务器
scp ~/projects/舞镜/_live/*.py root@47.242.80.65:/www/wujing-api/
scp ~/projects/舞镜/_live/*.html root@47.242.80.65:/www/wujing-api/static/

# 3. 重启服务
ssh root@47.242.80.65 "systemctl restart wujing-api && sleep 3 && \
  curl -s http://localhost:3006/api/health"

# 4. 公网验证
curl --resolve wujing.mylumee.app:443:47.242.80.65 -sk \
  https://wujing.mylumee.app/api/health
```

#### 交付清单

| 检查项 | 状态 | 备注 |
|--------|------|------|
| 代码无CVE（3个洞堵完） | ✅ | S5/S11/T5都修了 |
| 支付真人验证通过 | ✅ | Karen上线签字 |
| 功能30项全绿 | ✅ | 自检清单通过 |
| PostHog埋点有数据 | ✅ | dashboard显示实时用户 |
| 性能达标 | ✅ | 首页1.2s / health 200ms |
| 库存真实标注 | ✅ | 7支明确，76支占位清楚 |
| 文案全部校对 | ✅ | 无虚假宣传 |
| 服务器日志无ERROR | ✅ | `tail -f /var/log/wujing.log` |

---

## 改造成本总结

### 人力投入
| 角色 | 投入 | 工作量 |
|-----|------|--------|
| Claude Code (后端/前端/DevOps) | 15 个工作日 | 120 小时（含测试） |
| Karen (支付验收/决策) | 2 小时 | 真人扫码付款 + 签字 |
| QA/审计 | 3 个工作日 | 走查功能清单 |

### 技术成本
| 项 | 投入 |
|----|------|
| API token | ~30-50k tokens（代码改动+测试） |
| 服务器升配 | 无需（已加swap+资源笼子） |
| 第三方服务 | PostHog（如果需要付费tier）可选 |

### 时间规划
```
Day 0-1:   代码安全 + 支付验收 (4h + Karen 0.5h)
Day 2-4:   产品UX + 功能入口 (20h)
Day 5-7:   库存社交 + 埋点验证 (12h)
Day 8-14:  完整支付UX + 性能 (24h)
Day 15-21: 库存上线 + 最终校对 (16h)
Day 22:    部署 + 交付 (4h)

总计: 15 个工作日 (含并行QA)
```

---

## 达到10分的关键里程碑

当以下每一项都✅时，产品即达到10/10分：

### 安全与合规
- ✅ XSS零漏洞（_esc转义全覆盖）
- ✅ RCE零风险（magic byte校验生效）
- ✅ 鉴权无绕过（auth-first）
- ✅ 18+验证有效（首次弹窗阻止未成年）
- ✅ AI标识清晰（隐私政策可追踪）

### 产品体验
- ✅ 支付路径无断层（预览→支付→解锁顺畅）
- ✅ 加载态完整（无白屏、有进度条）
- ✅ 文案零杀伤力（"可选"而非"例如"）
- ✅ 信息层级清晰（主CTA明显）
- ✅ 社交信号充分（学过次数、评分都显示）

### 功能完整性
- ✅ 三入口接通（我的作品/班级/对比）
- ✅ 库存真实标注（7支明确，无虚标）
- ✅ 卡片下载可用（八拍/故事/记忆都可下）
- ✅ 分享功能完善（带水印、可追踪）

### 数据可见性
- ✅ PostHog全链路埋点（12个关键节点有数据）
- ✅ 漏斗分析清晰（上传→支付→解锁转化率可看）
- ✅ 用户行为可追踪（不再失明）

### 性能与稳定性
- ✅ 首页加载<1.5s（Lighthouse 85+分）
- ✅ /api/health <200ms（可持续监控）
- ✅ 拆解完成<120s（用户可以等）
- ✅ 0超时问题（subprocess timeout生效）
- ✅ 0资源泄漏（图片懒加载、字典定期清理）

### 商业闭环
- ✅ 支付真人验收通过（Karen签字）
- ✅ 免费配额可控（不烧钱漏洞）
- ✅ 库存健康可扩（已有3支可即时上线）
- ✅ 复购路径清晰（"我的作品"tab有数据）

---

## 风险与mitigation

| 风险 | 概率 | 影响 | Mitigation |
|-----|------|------|-----------|
| PostHog服务503持续 | 低 | 数据失明1周 | 改GA4或本地事件队列 |
| 服务器磁盘85%触发 | 中 | 上传功能断 | 清理旧用户视频或升配 |
| 支付渠道临时故障 | 低 | 收费中断<1h | 有Stripe备选通道 |
| /api/compare未来得及完成 | 低 | 功能延期 | 优先级改为P3，不阻塞上线 |
| 库存3支上线有bug | 低 | 改好后重新SCP | 本地完整测试后再推 |

---

## Karen的三个决策点

### 决策1️⃣: 第一天下午 — 支付链路通过
**内容**: 真人验收支付链路  
**输入**: 截图支付成功 + 拆解完整显示  
**输出**: 给权限切 WJ_FREE_MODE=0  
**时间**: 15分钟  

### 决策2️⃣: 第四天 — PostHog选择
**内容**: 确认PostHog能恢复还是改GA4  
**输入**: Claude Code诊断报告  
**输出**: 选用哪个方案  
**时间**: 5分钟  

### 决策3️⃣: 第十五天 — 最终交付签字
**内容**: 验收完整功能清单  
**输入**: 30项功能都✅ + 性能数据  
**输出**: 上线生产权限  
**时间**: 30分钟  

---

*方案生成: 2026-08-08 Claude Code | 改造开始日期: 即刻*
