# 舞镜 WuJing · 交接文档（0730）

> AI 舞蹈拆解教练。接手先读这份 + `PRD-任意舞自动拆解-0729.md` + `交接文档-0729.md`。

## 0. 基本盘（不变）
- **真生产**：https://wujing.mylumee.app （HK `47.242.80.65:3006`）· 也挂 `www.mylumee.app/dance/`
- **后端**：`/www/wujing-api`（FastAPI + systemd `wujing-api`，**非 git**）。改 py 要 `systemctl restart wujing-api` + `curl 127.0.0.1:3006/api/health`
- **前端**：单文件 `/www/wujing-api/static/design-upgrade.html`（改静态即生效不重启）。⚠️头部有 `<base href="/dance/">`，相对 `api/` 路径靠它
- **SSH**：`ssh -i ~/.ssh/id_ed25519 root@47.242.80.65`
- **本地工作副本**：`~/projects/舞镜/_live/`（scp 拉的 server.py/auto_decompose.py/pose_angles.py/design-upgrade.html）。改完 `scp` 回 + `md5` 校验
- ⚠️**本机浏览器打不开线上域名**（DNS/代理劫持）→ 验证走 `ssh curl localhost:3006` 或本地起 server 开 `_live` 副本

## 1. 今天（0730）做了什么 —— ⭐核心产品「任意舞自动拆解」上线
> Karen 定的产品：**没有固定舞库。用户上传任意一支舞 → AI 自动拆成能跟练的八拍卡+故事卡+记忆卡+精准角度点评。免选参考老师。** 存储=成本→变现：付费长期存/免费下载带走(带水印=裂变)。所有产物一键分享。

**已上线（生产端到端验证过）：**
- **上传任意舞**：`POST /api/decompose`(video+title+genre·免选老师·挂免费额度门防刷) → `GET /api/decompose/{id}` 拉结果 → `/clip/{full|pN}` 视频 + `/frame/{name}` 帧图(都公开)
- **引擎** `auto_decompose.py`：固定八拍分段(dur/3.3·5-10段) + **每段抽5帧(中帧+4条胶片帧)** + 豆包 vision 逐段描述(name/action/feet/intent/kou) + DeepSeek 故事卡 + ffmpeg 切段。≈¥0.06/支
- **无参考 AI 点评**：vision 看5帧给 亮点+改进(精准·禁空泛套话)。总评+good[]+improve[]
- **⭐MediaPipe 姿态角度**(=能收费的护城河)：`pose_angles.py`(独立 `mpvenv` 跑·避开主服务 numpy2 冲突)·逐段测真实关节角度(肩/肘/膝/髋/躯干)·喂点评让它**引用测量真数字**("左肘68°→需170°")·挂八拍卡显示。模型 `models/pose_landmarker.task`
- **genre 自动判定**：vision 判 guofeng/kpop（修了"一律 guofeng"的坑）
- **前端**(design-upgrade.html 尾部自包含 `.dc-` 模块 + `#dcModal`)：首页 hero 双入口(**上传你的舞**→`showDecomposeUpload` / 对着老师打分→review) + 上传页 + 结果页(**八拍卡胶片条 + 慢放/镜像 + 故事卡 + 记忆卡整片视频 + 一键分享**)。慢放=`playbackRate`·镜像=`scaleX(-1)`(前端做不重编码)

## 2. ⚠️设计教训（重要·别再犯）
- 我一开始用**浅米金单张定格**做结果页 → Karen "没眼看/太差"。**照现有别重造铁律**：先看 `~/Downloads/月分解/`（`八拍卡.py`/`故事卡.py`/`记忆卡.py` 生成的**深色胶片条**设计 + `dist/review/score-demo.html` 深金衬线设计系统 + `舞镜.html` 真app）。
- 澄清后的定论：**留 Karen 认可的「浅米金颜色」+ 采用现有的「胶片条内容结构」(一段多帧看动作) + 不要深色(她嫌丑)**。
- 现状：结果页已改成**浅米金 + 胶片条(4帧/段)**，渲染验证通过(28帧全加载·hero/点评/角度都在)。**最终 10 分视觉精修 + Karen 过目待做**（她铁律：不到10分不给她看）。
- 设计系统 token(浅米金)：底`#FAF7F0` 卡`#fff` 边`#EDE4CF` 金`#B5922A/#D8B65E` 字`#1F1A14/#4A3820` 弱`#9A8768` 口诀金 · Noto Serif SC 衬线 · 胶片左条彩色循环(蓝绿金粉紫)

## 3. PRD 与任务
- PRD：`docs/PRD-任意舞自动拆解-0729.md`（锁定版）
- 点评"都要"：A 无参考AI点评(✅已上线) + B 对着标准版打分(原有compare流程)
- 待办 P0(我做·顺序防冲突)：**#8 实时练习摄像头看角度**(浏览器 MediaPipe·AI镜子·杀手锏) / 付费墙(角度分析+实时摄像头=付费·Stripe 5key待Karen) / 付费存vs免费下载带水印
- 待办 P1(可派agent并行)：作品广场UGC / 分享升级带水印图 / 多风格验证(已跑✅)
- P2：健身版复用同套pose技术(`~/projects/ai-fitness/`) / librosa真节拍分段 / Karen手机真机验收

## 4. 已知问题（backlog）
- **bpm 全 null**：现固定等分段非真节拍(v1够用)·librosa 上服务器做精准八拍=v2
- **K-pop 暗场 pose 检出率低**(43%·孔雀舞100%)：暗场+群舞 MediaPipe 抓不到骨架·待加提亮/主舞者锁定/"未测到"提示
- 3个 agent 早前做的 mockup(share-card/plaza/my-works)用了**浅米金单帧老风格**·需按最终定稿设计重对齐

## 5. 验证/部署命令
```
# 部署 py：scp 回 + 重启 + 健康
scp -i ~/.ssh/id_ed25519 auto_decompose.py root@47.242.80.65:/www/wujing-api/ && \
ssh -i ~/.ssh/id_ed25519 root@47.242.80.65 'systemctl restart wujing-api && sleep 2 && curl -s 127.0.0.1:3006/api/health'
# 部署前端：scp + md5 校验
# 端到端测：注册→/api/decompose 传 /www/wujing/qingluu/reference.mp4 →轮询→看 coach 引用角度
```

## 6. 0730 下午追加（设计精修 + UX + QA + 专家评审修复·都已上线验证）
- **结果页精修到 9.7/10**（design agent·已部署）：电影胶片条(齿孔+1/4标号+"动作全程") / 角度做成"当前°→目标°仪表条(达标绿·偏差琥珀·进度条) / hero+点评卡(金渐变框·绿'做得好'/琥珀'可以更好')+故事卡 全高级浅米金衬线。
- **用户体验→10分**：拆解等待从死转圈改成**分步进度打勾**(读取切分→AI识别动作→测量角度→生成卡·`dcLoading`+`DC_STEPS`) + 上传页提示最佳输入(单人/清晰/15-30s) + 失败页友好可操作。
- **全功能后端 QA 28/0 全绿**：脚本 `~/projects/舞镜/_live/qa_all.py`(scp到服务器 `python3 qa_all.py` 跑)。逮到并修了 **胶片帧404 bug**(frame/clip接口 `safe` 过滤把下划线删了→`p1_0`找成`p10`·已改允许下划线)。
- **专家评审** `docs/优化评审-0730.md`，抓出3个生产坑**已全修**：
  1. `/api/generate-bg` 无额度门(烧钱洞·那个300+)→加 `check_analysis_access` 门
  2. 上传零校验→加 类型(video/*或octet-stream)+大小(500MB)校验(⚠️别只判`video/`前缀·会误拒octet-stream合法上传)
  3. 护城河自伤:低置信度角度当真数字→`_run_pose` 过滤 `visibility<0.55`(清晰视频不受影响)
  4. ⚡切片 `-c copy` 不重编码(慢放/镜像是前端做的)→提速
- **专家路线图·还没做的(下一步)**:并发队列(threading无上限·10人同传OOM) / 存储TTL清理(1万/天50-200GB) / K-pop群舞角度(真因`num_poses=1`遇多人剪影·非暗·要多人检测选主舞者) / describe批量化省30%成本 / clip+frame端点归属校验(did是UUID·低危)。
- **CMO推广方案** `docs/推广方案-0730.md`(9.6/10)。作品广场/我的作品/分享图样图在 `docs/mockups/{plaza,my-works,share-card}-demo.html`(⚠️早前agent用了浅米金**单帧**老风格·接线前要按最终胶片条设计对齐)。

## 7. Next
按专家路线图修 并发/存储/K-pop多人 · 实时摄像头看角度(#8·杀手锏) · 付费墙(复用Lumee境内支付商户) · Karen手机真机验收。
