# 舞镜 · 交接文档（0731 收费+成功页 Session）

> **Commit**: `23bad61`（本次）+ 前7个 commit（见下方历史）
> **代码总量**：6159 行（前端 3960 + pay.py 1121 + server.py 634 + models.py 171 + auto_decompose.py 273）
> **线上**：`https://wujing.mylumee.app`
> **推送命令**：`git push origin auto-decompose-mediapipe-0730`（SSL 偶发失败时手动跑）

---

## 一、本次 Session 做了什么（commit 23bad61）

| 模块 | 改动 |
|---|---|
| **Stripe recurring 订阅** | `mode: payment` → `mode: subscription` + Price ID接通，真自动续费 |
| **月卡价格** | $7.99 → $9.99（前后端同步） |
| **月卡成功页** | 金色渐变头部 + 权益列表(3条) + slideUp动画 + 实时到期日 |
| **单次付款后** | 绿色 ✓ flash(500ms) → 28s倒计时 → 步骤双语优化 |
| **Stripe webhook别名** | `/stripe/webhook` → `/api/pay/stripe/webhook`（对应Stripe Dashboard注册路径）|
| **webhook 3事件** | `checkout.session.completed` / `invoice.paid`(续费) / `customer.subscription.deleted`(取消) |
| **DB新增列** | `users.stripe_customer_id` + `users.stripe_subscription_id` |
| **Slim 支付宝** | CN用户付款先选渠道（💚微信/🔵支付宝），commit `c66a04c`，明天 Vercel 额度重置后部署 |

---

## 二、Stripe 配置（已完成）

| 项目 | 值 |
|---|---|
| Price ID 月卡 | `price_1TzApKEAXrE2YgcrjCsVgNPo`（$9.99/月 recurring）|
| Price ID 单次 | `price_1TzApQEAXrE2Ygcr5HC1yUj1`（$1.99 one-time）|
| Webhook endpoint | `https://wujing.mylumee.app/stripe/webhook` |
| Webhook 事件 | `checkout.session.completed` / `invoice.paid` / `customer.subscription.deleted` |
| Webhook secret | `whsec_tiP54ILknIsXNL1lMcyv01G3i4VZ7qiR`（已写入服务器 override.conf）|

---

## 三、定价表

| 方案 | 中国 🇨🇳 | 美国 🇺🇸 |
|---|---|---|
| 单次拆解 | ¥9.9 | $1.99 |
| 单次复购（老用户）| ¥7.9 | $1.59 |
| 月卡 | ¥39/月 | $9.99/月 |

---

## 四、完整 Commit 历史（按时间倒序）

| Commit | 内容 |
|---|---|
| `23bad61` | Stripe recurring + 月卡成功页 + 付款后UX + webhook链路（本次）|
| `5d0790b` | 到10分：裂变真实归因+折扣后端校验+留存回访钩子+弹层英文化 |
| `a3b9599` | CMO钩子升级：裂变/留存/续费/英文版全到10分 |
| `5fedeb3` | 最终上线交接文档(7commit总结+当前状态+loop机制+待Karen决策) |
| `f7b054c` | 0731收尾：英文版+游客作品+XSS+价格+后台轮询+class安全区+loop机制 |
| `9176a40` | 增长+英文版+UX收口：三部门审计P0/P1落地 |
| `ac14bed` | 交接文档更新为上线前全绿状态 |
| `a072bd4` | 上线前安全审计修复：双agent对抗式审计P0全堵 |
| `6d7ccbe` | 底部导航+班级打通+首页收敛+支付验证+清理 |
| `0476a77` | 真人开发交接文档 |
| `74261c1` | UX全面修复到10分 + 代码整理可交接真人 |
| `db80271` | 0730最终交接文档 |
| `ce2f8d0` | base href移除+首页文案全部对齐decompose+MV暂下 |
| `e736a02` | 批量银灰→暖金+CTA统一+decompose持久化 |
| `58bec83` | 全面修复→10分：CSS变量映射+UX P0修复 |
| `7f472e8` | 支付前端上线(抄Lumee payX)+新模块收口 |
| `9ca3633` | 补5部门上线前优化清单文档 |
| `96b12e4` | P0变现+独舞/群舞/埋点个人库实现+免费版上生产 |
| `21e7291` | 设计精修10分+UX分步进度+QA全绿+专家评审3坑修复 |
| `225384d` | 任意舞自动拆解+MediaPipe角度点评上线 |

---

## 五、上线唯一阻塞（Karen 做）

```bash
# 1. 扫码付 ¥9.9 真单，验证全链路（上传→扫码→回调→出卡）
# 2. 验证通过后切付费模式：
ssh root@47.242.80.65 "sed -i 's/WJ_FREE_MODE=.*/WJ_FREE_MODE=0/' \
  /etc/systemd/system/wujing-api.service.d/override.conf && \
  systemctl daemon-reload && systemctl restart wujing-api"
```

---

## 六、服务器运维

```bash
# 健康检查
curl https://wujing.mylumee.app/health

# 查日志
ssh root@47.242.80.65 "journalctl -u wujing-api -n 50"

# 紧急回滚
ssh root@47.242.80.65 "cd /www/wujing-api && git stash && systemctl restart wujing-api"

# 紧急回滚SOP
~/projects/舞镜/docs/us-launch/紧急回滚SOP.md
```
