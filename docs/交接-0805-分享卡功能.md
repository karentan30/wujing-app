# 舞镜 分享卡功能交接文档 · 0805

## 今日完成

### 1. 八拍卡 / 故事卡 / 记忆卡 分享升级（7→10分）
文件：`_live/design-upgrade.html`

| 修复 | 位置 | 说明 |
|------|------|------|
| 缩略图预览 | ~3832行 `.dc-card-dl` | 下载前先看到卡长什么样，点击率↑ |
| iOS下载修复 | `dcSaveCard()` | `toDataURL+a.click` → `toBlob+createObjectURL+appendChild/removeChild` |
| 字体等待 | `dcSaveCard()` | `document.fonts.ready.then()` 包裹绘制，中文不再乱字形 |
| 下载成功toast | 各下载 `onclick` | "已保存，快去小红书发吧！" |
| 故事卡图片分享 | `dcShare('story')` | `navigator.share` 带图片文件，不支持则fallback纯文字 |

### 2. 评分结果"分享成绩"卡（新功能，0→10分）
文件：`_live/design-upgrade.html`，`reviewShareCard()` 函数 ~2181行

- 按钮：金色渐变"分享成绩"，在评分结果action区
- Canvas：1080×1350（4:5，适配微信/小红书feed）
- 内容：总分大环 + 动作姿态/节拍契合/镜像对齐三维度进度条 + 品牌
- 修复：`_rrect` polyfill 替代 `ctx.roundRect`（旧Android不崩溃）
- `document.fonts.ready.then()` 字体加载后再绘制
- 下载前"生成中..."，成功后"成绩卡已保存！可发微信/小红书"

### 3. 待做
- `bazi-en.html` 英文版命盘卡（善缘那边的事）
- 部署到 HK 服务器（47.242.80.65）：`scp _live/design-upgrade.html karen@47.242.80.65:~/wujing/_live/`

## 文件清单
- `_live/design-upgrade.html` — 所有前端改动
- `_live/server.py` — 评分接口（review）
- `_live/review_compare.py` — AI对比评分逻辑
- `_live/auto_decompose.py` — 自动拆解流程
- `_live/cards/*.py` — 卡片生成脚本

## 服务器
- HK：47.242.80.65，systemd托管，`sudo systemctl restart wujing`
- 域名：wujing.mylumee.app（Namecheap DNS）
