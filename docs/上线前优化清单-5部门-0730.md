# 舞镜 · 5 部门上线前优化清单（0730）

> UX 诊断现状 5.5/10，核心问题是「5 张互不认识的样图 + 1 个旧壳真 App，不是一个产品」，到 10 分 90% 是「打通 + 统一」。以下为 CMO/设计/工程/合规/QA 五部门的优化清单。

## 一、CMO 营销就绪（营销包 8.5 / 放量就绪仅 5）
**放量 5 闸（全绿才投付费流，否则烧钱）：**
1. 首页 3 处死 CTA（`href=#`）接真上传/付款接口
2. 真 App 接进 9.9 付费门
3. 大陆友好短链配置
4. plaza 假互动数据（1.2k/3.4k/18k）删除
5. 角度数字「真实拆解截图存证」流程（禁编）

**其他：** 首页付费 CTA 移到成果展示末尾（先看价值再付）；Hero 加次入口「先看一支拆好的例子↓」降门槛；营销主张已收敛（拆解卡为主线），但**不能加剧和真 App 的定位分裂**；达人置换体验不展示 plaza 当前版。

## 二、设计总监：全站视觉统一 + 旅程打通（重灾区）
**金色收敛**：现有 5 个金值混乱（#C8A45C / #C8A96E / #E4C877 / #C4A052），全部收敛到 **#B5922A / #C9A94A**。

**统一 token（以 home-v2 `:root` 为唯一真理源）：**
`--bg:#FAF7F0 / --card:#fff / --edge:#EDE4CF / --gold:#B5922A / --gold-soft:#C9A94A / --ink:#1F1A14 / --font-serif:"Noto Serif SC"`

**各页改法：**
- **真 App `design-upgrade.html`（改动最大·P0）**：删液态金属灰银背景 → `#FAF7F0`；`--warm` 铜棕 → `--gold`；SF Pro → Noto；玻璃卡 → 白卡 + 米金边；**舞台视频区保留深色（唯一允许深色处）**。
- **AI 点评 `dance_decompose.html`**：`:root` 换 token，金 `#c8a96e` → `--gold`，标题加 serif。
- **群舞 `report.html`**：金 `#C8A45C` 收敛（已最接近北极星）。
- **个人库 `my-works` / 广场 `plaza`**：金 `#E4C877` 收敛 + 删假数据。

**旅程打通**：首页 → 上传 → 免费预览 → 解锁 9.9 → 付款 → 出卡 → 慢放跟练 → 点评 → 个人库进步曲线，每个跳转要有衔接。

## 三、工程 CTO（上线前必修）
- **支付上生产**：回调 URL 公网可达 + Caddy 别给 `/api/pay/*` 加鉴权（否则平台回调 401）+ 密钥走 env 不硬编。
- **群舞并发队列**：已在 `group_review.py` 实现（BoundedSemaphore 限流，修 10 人同传 OOM）。
- **前端 CTA 接真接口**（现 `href=#`）。
- **旧 orders 表冲突**：已解（DROP 旧表，pay.py 重建，db 备份）。
- 2 核资源笼子下拆解排队/超时兜底。

## 四、合规 CSO（红线，上线前必堵）
- **plaza 假数据 1.2k/3.4k/18k**：必删（已在 `my_works.py` 实现，只展示真实字段）。
- **假点评弃用**：现成 SSIM+随机模板会编数字（已在 `review_compare.py` 换真姿态实测）。
- **不暴露 MediaPipe**：已全站改「舞镜 AI」。
- **儿童数据**（舞室多儿童）：家长同意 + 最小留存，写进 B 端合同。
- **付费诚实**：要拆付 9.9 说清、不虚假促销。
- 用户视频隐私：存储/不滥用。

## 五、QA（功能走查，别让老板当第一个发现 bug 的人）
- 抓：死按钮 / `href=#` / 静默失败 / 加载态缺失 / 原生 alert / 未渲染。
- 真点全流程：无需登录上传 → 付费门 → 拆解出卡 → 慢放/镜像 → AI 点评 → 群舞 → 登录/付款 → 各种边界（网络慢/大文件/重复点/返回）。

---
> 完整原文见当日 workflow 输出。落地时按此清单在「收口整合」阶段逐条执行。
