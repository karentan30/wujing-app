# 舞镜 交接文档 · 0805

> 接手先读本文。承接 0804 交接。今日核心：**付款通道上线**。

---

## 今日完成

### 收款通道接通（Hub 代理模式）✅

**方式**：舞镜无独立商户号，改走 Lumee Hub 代理收款（复用 Lumee 已有的微信/支付宝商户）。

**改动文件**：`pay.py`

**逻辑**：
- 微信/支付宝 keys 为空时自动 fallback → `HubPay('wujing')` → Lumee Hub → 真实收款
- Hub order_no 作为本地 `out_trade_no`，`channel` 标记为 `hub_wechat` / `hub_alipay`
- 前端轮询 `GET /api/pay/status?out_trade_no=xxx` → 自动向 Hub 查单 → 付成功即解锁舞蹈

**验证**：
```bash
# 微信 — 返回真实 weixin:// 二维码链接 ✅
curl -X POST http://localhost:3006/api/pay/wechat/create -d '{"dance_id":"vid_qingshan"}'

# 支付宝 — 返回真实 qr.alipay.com 二维码链接 ✅
curl -X POST http://localhost:3006/api/pay/alipay/create -d '{"dance_id":"vid_qingshan"}'
```

**WJ_FREE_MODE 已改 0**（服务器 start.sh），用户上传后需付 ¥9.9 才解锁拆解卡。

---

---

## 一、当前生产状态（从 0804 延续）

| 项 | 值 |
|---|---|
| 域名 | `https://wujing.mylumee.app` |
| 服务器 | HK 47.242.80.65，端口 3006 |
| 后端目录 | `/www/wujing-api/` |
| 服务管理 | `systemctl status wujing-api` |
| 本地工作副本 | `~/projects/舞镜/_live/`（非 git，scp 推服务器）|
| 免费模式 | `WJ_FREE_MODE=1`（付款闭环未通，免费跑）|

**重启命令**：
```bash
ssh root@47.242.80.65 "systemctl restart wujing-api && sleep 3 && curl -s http://localhost:3006/api/health"
```

**部署命令**：
```bash
scp ~/projects/舞镜/_live/*.py root@47.242.80.65:/www/wujing-api/
scp ~/projects/舞镜/_live/design-upgrade.html root@47.242.80.65:/www/wujing-api/static/
scp ~/projects/舞镜/_live/static/home-v2.html root@47.242.80.65:/www/wujing-api/static/
ssh root@47.242.80.65 "systemctl restart wujing-api"
```

---

## 二、舞蹈库状态

| 类别 | 数量 |
|---|---|
| ✅ Ready（可用） | **7 支** |
| ⏸️ Coming Soon（占位） | 76 支 |
| **总计** | **83 支** |

### Ready 舞蹈（7 支）
| ID | 曲名 | 风格 |
|---|---|---|
| vid_qingshan | 青山烟雨客 | 古风 |
| vid_2130688639 | 虞兮叹 | 古风 |
| vid_565648474 | 群舞·琵琶曲 | 古风 |
| vid_1488273297 | 清明雨上 | 古风 |
| vid_kpop_newjeans | Hype Boy | K-pop |
| vid_kpop_blackpink | Pink Venom | K-pop |
| vid_kpop_bts | Dynamite | K-pop |

### 本地已完成但未推服务器的舞蹈（3 支）
| ID | 曲名 | 段数 | 状态 |
|---|---|---|---|
| vid_1679971642 | 虞兮叹（另版） | 9 | 本地 completed，未推服务器 |
| vid_563626483 | 春满园 | 6 | 本地 completed，未推服务器 |
| vid_2130688639 | 孽 | — | 本地 completed，未推服务器 |

> ⚠️ 这 3 支在 `_live/data/` 有完整拆解数据 + 八拍卡，但未 scp 到服务器，也未更新 `library_index.json` 为 ready。**下次接手可直接上线。**

**上线步骤**（以春满园为例）：
```bash
# 1. 推数据目录
scp -r ~/projects/舞镜/_live/data/vid_563626483 root@47.242.80.65:/www/wujing-api/data/

# 2. 更新 library_index.json 里该条 status 改 ready，推上去
scp ~/projects/舞镜/_live/library_index.json root@47.242.80.65:/www/wujing-api/

# 3. 重启（可选，静态数据不需要）
ssh root@47.242.80.65 "systemctl restart wujing-api"
```

---

## 三、功能矩阵（完整）

| 功能 | 状态 | 备注 |
|---|---|---|
| 上传任意舞蹈 | ✅ | 无需登录 |
| 八拍卡自动生成 | ✅ | phrases 字段 |
| 故事卡 | ✅ | story 字段 |
| 记忆卡 | ✅ | memory 字段，含整支视频 |
| AI 教练点评 | ✅ | coach 字段，真实角度数字 |
| 慢放 0.5x + 镜像 | ✅ | 前端 CSS/playbackRate |
| AI 比对点评（solo） | ✅ | 有角度→对比分；无角度→vision 降级 |
| 营销首页（home-v2） | ✅ | `/` 首页，动态拉已拆解数 |
| 登录/注册 | ✅ | JWT |
| 付费闭环 | ❌ | 商户号未配，WJ_FREE_MODE=1 |
| 个人作品库 | ⏸️ | 后端 my_works.py 完整，**前端无入口** |
| 群舞班级 | ⏸️ | 后端 group_review.py 完整，**前端无入口** |
| PostHog 埋点 | ❌ | 完全盲飞，接了才知留存 |
| AI 对比视频生成 | ⏸️ | compare-video-script-v5.py 本地跑通，后端 /api/compare 已建，前端无入口 |

---

## 四、待办（优先级）

### P0 — 开收钱前必做
1. **申请微信/支付宝商户号**（需营业执照，审核数天）
2. 配商户 key 到 `/www/wujing-api/start.sh` + 改 `WJ_FREE_MODE=0`
3. 微信商户后台授权回调域名 `wujing.mylumee.app`

### P1 — 留存关键
4. **PostHog 埋点**（接入 Project Key，埋上传/拆解完成/比对点评/付费节点）
5. **个人作品库前端**（后端 `my_works.py` 完整，前端加入口即可）

### P2 — 内容扩充（下支舞蹈）
6. **推送本地 3 支已完成舞蹈上线**（春满园 / 孽 / 虞兮叹另版，步骤见上）
7. 27 支无角度舞蹈重跑 pose detection → 补角度数据 → AI 比对点评质量大幅提升

### P3 — 增长
8. 群舞班级前端（B 端，高客单，后端已完整）
9. AI 对比视频前端入口（`/api/compare` 已建）
10. 社交分享带水印完善

---

## 五、关键文件速查

| 文件 | 作用 |
|---|---|
| `server.py` | 主服务，所有路由注册 |
| `auto_decompose.py` | 拆解核心，豆包 vision 调用 |
| `review_compare.py` | AI 比对点评 |
| `my_works.py` | 个人作品库（后端完整） |
| `group_review.py` | 群舞班级（后端完整） |
| `pay.py` | 付款逻辑（商户号配好后取消注释） |
| `static/design-upgrade.html` | 主 App 前端 |
| `static/home-v2.html` | 营销首页 |
| `library_index.json` | 舞蹈库索引（ready/coming_soon 开关在这） |
| `docs/compare-video-script-v5.py` | AI 对比视频本地生成脚本 |

---

## 六、成本参考

| 操作 | 约成本 |
|---|---|
| 一次拆解（8 次 vision + 1 次 DeepSeek） | ≈ ¥0.06/支 |
| 一次比对点评 | ≈ ¥0.02/次 |
| 生图（/api/generate-bg） | ≈ ¥0.5–1/张，慎用 |

---

## 七、坑 & 注意事项

- **本机 DNS 被代理劫持**：验证公网用 `curl --resolve wujing.mylumee.app:443:47.242.80.65 -sk https://wujing.mylumee.app/api/health`
- **systemd 资源笼子**：CPUQuota=100%（1 核上限）+ MemoryMax=1.2G，防拖死 Lumee
- **旧 analyze.py**：含 SSIM 假评分，已断开所有入口。不删、不接
- **磁盘红线**：目前 77%，触发信号 = 磁盘 > 85%，届时清 `/www/wujing-api/data/` 旧用户视频或升配
- **library_index.json 有 83 条**：76 条是占位 coming_soon，改 status 为 ready 即上线，不需要重跑拆解
