# 舞镜 · 真人开发交接文档（0731）

> 本文档写给接手舞镜的真人开发者。先读这个，再读 `交接-0730最终.md` 和 `UX全流程诊断-0730.md`。
> 代码已经整理到「clone 下来就能跑、线上就是这份代码」的状态。

---

## 一、项目一句话

**AI 舞蹈教练 H5**：用户上传一段舞蹈视频 → AI 自动拆成「八拍卡 + 故事卡 + 记忆卡」→ 支持慢放/镜像跟练 + AI 逐帧角度点评 + AI 教练建议。商业化：上传免费，拆解结果 ¥9.9/支舞。

## 二、代码在哪

| 位置 | 说明 |
|---|---|
| `~/projects/舞镜/` | git 仓库（分支 `auto-decompose-mediapipe-0730`） |
| `~/projects/舞镜/_live/` | **唯一真源**。所有运行代码，clone 下来就能跑 |
| `~/projects/舞镜/_live/design-upgrade.html` | 唯一前端页面（单页应用，~2900行，JS/CSS/HTML 全在一个文件） |
| `~/projects/舞镜/_live/server.py` | FastAPI 入口（挂载所有路由 + 根路由返回 design-upgrade.html） |
| `~/projects/舞镜/_live/pay.py` | 支付（微信NATIVE/支付宝当面付/Stripe Checkout，含验签+幂等+履约） |
| `~/projects/舞镜/_live/auto_decompose.py` | 核心：任意舞自动拆解（音频切拍+MediaPipe姿态+AI生成卡内容） |
| `~/projects/舞镜/_live/review_compare.py` | 独舞点评（5条 /api/solo/*） |
| `~/projects/舞镜/_live/group_review.py` | 群舞/班级（13条 /api/class/*） |
| `~/projects/舞镜/_live/my_works.py` | 个人舞库/广场 |
| `~/projects/舞镜/_live/analytics.py` | PostHog 埋点 |
| `~/projects/舞镜/docs/` | PRD / UX诊断 / 优化清单 / 历史交接 |

**当前 commit**：`74261c1 舞镜 0731: UX全面修复到10分 + 代码整理可交接真人`

## 三、技术栈

- **后端**：Python 3.10 + FastAPI + uvicorn，SQLite（`wujing.db`，11张表），单进程
- **前端**：原生 HTML/CSS/JS 单文件 SPA（无框架、无构建、无 npm）
- **姿态引擎**：MediaPipe（独立 venv 子进程，`_live/auto_decompose.py` 调用）
- **AI 内容生成**：ARK（豆包）Vision + DeepSeek（key 在服务器 start.sh）
- **埋点**：PostHog
- **部署**：systemd `wujing-api` + Caddy（自动HTTPS），HK 服务器 47.242.80.65

## 四、本地跑起来

```bash
cd ~/projects/舞镜/_live
python3 -m venv venv && source venv/bin/activate
pip install fastapi uvicorn python-multipart requests
# 环境变量参考服务器 /www/wujing-api/start.sh
python -m uvicorn server:app --port 3006
```

注意：
- 本地测试想不碰生产数据 → 设 `WUJING_BASE_DIR=/tmp/wujing-local`（server.py 已支持）
- 拆解引擎需要 MediaPipe venv（服务器 `/www/wujing-api/mpvenv/`）和 ARK/DeepSeek key
- 支付需要商户号 key（见五），本地无 key 时支付接口返回 503 不崩

## 五、线上部署

| 项 | 值 |
|---|---|
| 服务器 | `ssh root@47.242.80.65`（阿里云 HK） |
| 代码目录 | `/www/wujing-api/` |
| 服务 | `systemctl restart wujing-api` |
| 日志 | `journalctl -u wujing-api -n 50 --no-pager` |
| 域名 | `https://wujing.mylumee.app`（Caddy 配置） |
| 数据库 | `/www/wujing-api/wujing.db`（sqlite3 直接查） |
| 支付 key | `/etc/systemd/system/wujing-api.service.d/override.conf`（复用 Lumee 商户号） |
| 环境变量 | `/www/wujing-api/start.sh`（ARK/DeepSeek/JWT） |

**部署流程（改完代码后）：**
```bash
scp _live/*.py root@47.242.80.65:/www/wujing-api/
scp _live/design-upgrade.html root@47.242.80.65:/www/wujing-api/static/
ssh root@47.242.80.65 "systemctl restart wujing-api"
```
改前端只传 `design-upgrade.html`，改后端传对应 py。

**切免费/付费模式：**
```bash
# 1=免费（上传即拆，不收费）0=付费（要拆付¥9.9）
ssh root@47.242.80.65 "sed -i 's/WJ_FREE_MODE=.*/WJ_FREE_MODE=0/' /etc/systemd/system/wujing-api.service.d/override.conf && systemctl daemon-reload && systemctl restart wujing-api"
```

## 六、当前线上状态

- ✅ 上线运行中：上传→拆解→出卡→AI点评 全链路正常（已回归验证）
- ✅ 支付三个渠道下单接口均通（Stripe URL 实测可开）
- ⚠️ **但从未真实收过一分钱**：现在是 `WJ_FREE_MODE=1` 免费模式
- ✅ UX 已完成 10 分轮修复（配色统一/触摸目标/加载态/支付信任/死链清理等 22 项）

## 七、给真人的待办/已知问题（按优先级）

### 🔴 P0 上线前必须做
1. **支付真单验证**：切 `WJ_FREE_MODE=0` 后，真人扫微信/支付宝付 ¥9.9，验证 回调→验签→翻paid→履约生成拆解卡 全链路。这是产品上线的最后一道闸。
2. **WJ_FREE_MODE 切 0**：商户号已就位，验证通过后切付费。之前一直免费是为了先拉真实用户。

### 🟠 P1 体验断点（UX评审已列，做完才真正10分）
3. **底部导航缺失**：首页无法直达「我的作品/广场/班级」。需在 design-upgrade.html 加底部 tab，但**先确认这三个功能页都已接入主H5**（现在是独立路由 `my_works.py`/`group_review.py`），避免做死按钮。
4. **首页信息层级**：7个 section 太长，核心动作「上传」被说服性内容稀释。建议收敛为「上传→结果预览→三步说明」。
5. **骨架屏**：首页舞库列表、拆解加载目前内容替换直接闪白，需骨架占位。
6. **Stripe 跳转恢复**：从 Stripe 返回后依赖 localStorage 恢复拆解状态，微信内置浏览器偶发不可靠。
7. **`/api/pay/hub/*` 遗留路由**：Lumee 收款中台已无此端点（404），`hub_router.py`/`hub_pay.py` 是废弃路径，可删或加 try 优雅报错。不影响 `pay.py` 原生支付。

### 🟡 P2 优化
8. **分享增强**：当前复制文案/系统分享，小红书/抖音需要「生成长图」。
9. **旧 review 流清理**：`server.py` 里的 `/api/upload`、`/api/review/*` 等旧端点已不被前端使用，可移除。
10. **AI 点评关联拍数**：coach.improve 现在是泛化文本，理想是关联到具体第几拍。

## 八、关键坑（前人踩过）

| 坑 | 解决 |
|---|---|
| `<base href="/dance/">` 导致 API 404 | 已移除 |
| `overflow:clip` 页面不能滚动 | 已修 → `overflow-y:auto` |
| CSS 变量未定义（180+处） | `:root` 加了银灰→金色兼容映射 |
| Stripe 跳转丢 decompose 状态 | localStorage 保存/恢复 |
| 支付 key 硬编码 | 走 systemd override.conf，不提交 git |
| 2核服务器拆解 OOM | 3G swap + systemd 资源笼（CPU 100%/Mem 1.2G）+ 群舞 BoundedSemaphore 限流 |
| 拆解大视频内存炸 | `/api/decompose` 500MB 上限 + 后台线程跑 |
| MediaPipe 不暴露 | 全站文案用「舞镜AI」 |

## 九、合规红线（产品层，别改掉）

- **无假数据**：广场/首页禁造假互动数字（已删）
- **付费诚实**：上传免费、拆解¥9.9，页面明示，不虚假促销
- **用户视频隐私**：不滥用、存储最小化
- **儿童数据**：舞室多儿童，B端合同要家长同意条款

---

> 有问题先 `git log --oneline` 看历史提交，每步改动都有中文说明。需要我补充任何部分，随时说。
