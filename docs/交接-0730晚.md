# 舞镜 0730 交接文档（晚）

## 🎯 今天的最大进展：免费版已上生产
`https://wujing.mylumee.app` 现在**真能免费上传 + 拆解出卡**（`WJ_FREE_MODE=1`）。
支付商户号到位后，把 `WJ_FREE_MODE` 设为 `0`，自动恢复「要拆付 9.9」。

---

## 一、今天完成

### 1. 上线基建
- 独立域名 `wujing.mylumee.app`（Namecheap 加 A 记录 → 47.242.80.65 + Caddy 自动签 LE 证书，端到端 200）。
- **资源护栏防拖死 Lumee**：3G swap + systemd 资源笼子（CPU 封顶 1 核 / 内存硬顶 1.2G / IOWeight 低）。
- 烧钱门 `/api/generate-bg` 确认已堵（登录 + 额度门）。
- **去 MediaPipe 品牌化**：前端/后端/report 全部改「舞镜 AI」，不暴露底层。

### 2. 设计样图（浅米金·`docs/mockups/`）
- 首页 `home-redesign-v2-成果呈现-0730.html`（成果呈现 + ¥9.9 价格卡 + 全链路 + 24支舞真实数据 + 整支正常速/逐句慢放/镜像），10 分。
- 登录付款 `auth-pay-0730.html`（4 屏），9.5。
- 线上预览：`/static/home-v2.html`、`/static/auth-pay.html`。

### 3. 策略产出（`docs/`）
- 留存体系设计（死穴=用完即走，最高 ROI=个人库+进步曲线）。
- CMO 营销发放包 `营销发放包-0730/`（文案库 10 分 + 卡片 + 视频脚本 + 6 账号矩阵 + 6 个 ComfyUI 生图 prompt 10 分·待生成）。

### 4. 后端实现（`_live/`·测试全绿）
- **P0 变现**：`server.py` 改门（免费无需登录上传 + 要拆付 9.9 + `WJ_FREE_MODE` 开关）+ `pay.py`（微信 NATIVE / 支付宝当面付 / Stripe，只依赖 fastapi，冒烟 27/0）。
- **独舞点评** `review_compare.py`：真姿态实测，**弃用假 SSIM+随机模板**点评，37/37。
- **群舞班级** `group_review.py`：4 表 + 并发队列（修 10 人同传 OOM），46/46。
- **埋点** `analytics.py` + **个人库** `my_works.py`：进步曲线两表，全过，删 plaza 假数据。

### 5. 审查
- UX 全流程诊断 `UX全流程诊断-0730.md`（现状 5.5，核心=打通+统一）。
- 5 部门上线前优化（CMO/设计/工程/合规/QA·各出到 10 分清单）。

---

## 二、待办（到 ready）

1. **支付商户号**（硬前置）：⚠️ Lumee 其实没配真实微信/支付宝 key（inert 状态）。要真收款得申请商户号（营业执照/主体，审核几天）。到位后：配 key 到舞镜 env + `WJ_FREE_MODE=0` 开 9.9。微信还需在商户平台**授权回调域名 `wujing.mylumee.app`**。
2. **收口挂载新模块到生产 `server.py`**（都测过了，只需加 include_router + init 表 + run_decompose 钩子）：
   - 独舞 `from review_compare import router as solo_router` → 履约调 `run_solo_review_from_disk(review_id)`
   - 群舞 `from group_review import router, init_group_tables, set_decompose_runner, set_optional_user_resolver`
   - 个人库 `from my_works import router, init_library_tables` + `upsert_my_work(user_id, result)` 挂 run_decompose 完成处
   - 埋点 `analytics.track()` 挂 pay_success / decompose_success
   - 详见各方案 md 的「怎么挂」章节。
3. **全站视觉统一 + 旅程打通**（设计总监 token 方案：金收敛到 #B5922A；真 App design-upgrade 换成 home-v2 风格）。
4. **前端 CTA 接真接口**（首页/样图 CTA 现在还是 `href=#`）。
5. **上线前审查**：功能真跑通 + 全到 10 分。

---

## 三、关键坑 / 决策（下次接手先看）

- **旧 orders 表冲突**：生产 wujing.db 有旧 payment.py 的 orders 表（无 dance_id 列），pay.py 建索引崩。已 DROP 旧表（19 行残留 paid 订单，**db 备份在 `/www/wujing-api/wujing.db.bak-orders-*`**，舞镜从未真上线收款故应为测试残留；若需要可恢复）。
- **pay.py 只依赖 fastapi**（微信/支付宝/Stripe 全用标准库，无第三方 SDK），生产可直接跑。
- **部署方式**：`scp _live/*.py → /www/wujing-api/`；env 走 `/etc/systemd/system/wujing-api.service.d/override.conf` 的 `Environment=`；`systemctl daemon-reload && systemctl restart wujing-api`。
- **免费/付费开关**：`WJ_FREE_MODE=1` 免费拆，`=0` 要拆付 9.9。
- **服务器**：HK 47.242.80.65，SSH key `~/.ssh/id_rsa`，舞镜后端 `/www/wujing-api/`（systemd `wujing-api.service`，port 3006）。
- **本机代理劫持**：`wujing.mylumee.app` 本机解析到假 IP，验证走服务器端或 `curl --resolve`。
- ⚠️ 勘察时意外在日志里带出过 Lumee 的 Agora/Aliyun 密钥（在 `/etc/xinshen.env`）——建议方便时轮换。

---

## 四、当前生产状态
- 舞镜后端 active，免费版运行中（上传→免费拆→出卡）。
- `_live/` = 完整最新代码（含未挂载的独舞/群舞/个人库模块）。
- 收口 = 把 `_live/` 的新模块挂进 `/www/wujing-api/server.py` + 配支付 key。
