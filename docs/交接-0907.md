# 舞镜 · 交接 0907

## 做了什么(一句)
修了4个静默故障 + 部署路径坑,记忆功能真上线,报告+主app UI 做到10分,支付走通中台,加了带货认穿搭上游,拆解提速。全部已 commit+push(分支 `launch-quality`,commit db90ee2→1807149)。

## 🔴 红线坑(这次踩的,必记)
1. **部署路径**:`/app`→`static/design-upgrade.html`、`/`→`static/home-v2.html`、`/report`→`static/report.html`。**serve自 static/ 不是 root!** 之前一路 scp 到 root 没生效。Python(server/引擎/pay)从 root 导入=对。改HTML必 scp 到 `/www/wujing-api/static/`。
2. **fetch递归卡96%(真凶·已彻底修)**:`design-upgrade.html` 有**两处**fetch包装(1843加设备头 + 5210加超时)**都用同名全局 `var _origFetch`**→第二处重赋值致第一处自引用→无限递归→栈溢出→pollDecompose崩→结果永远卡"还需1s|96%"(**真用户付款后也卡·严重**)。修=第二处改独立名 `_origFetch2`(链式:超时→设备头→原生)。真机《关山酒》验证:结果完整渲染。⚠️再有fetch包装必须用唯一变量名。
3. **拆解 signal 崩**:`auto_decompose.run_decompose` 的 `signal.SIGALRM` 只能主线程,而 server/pay 全在 Thread 里调→非主线程抛异常→拆解全崩(坏~1月)。已改仅主线程启用。
4. **DeepSeek 活 key = `DS_KEY`(sk-9385)**;start.sh 里的 `DEEPSEEK_API_KEY`(尾022b)是 402 死账号。故事卡曾读死key。
5. **支付走 Lumee 中台内网**:`HUB_BASE_URL=http://127.0.0.1:8765`(在 systemd override.conf),**不能用 www.mylumee.app**(被 Cloudflare 1010 当爬虫拦→403)。
6. **WJ_FREE_MODE=0**(付费态,override.conf)。测试临时开1记得设回。

## 现状(线上健康)
- `/`、`/app`、`/report` 全200。记忆功能(★重复/速记攻略/视频浮层)已真上线。
- 支付:微信+支付宝真二维码(走中台)。拆解:28s舞约58秒(已并行提速)。
- 认穿搭:decompose 结果带 `outfit` 字段(衣/妆/饰+淘宝关键词)。
- 报告页:班级+个人视图设计到10(免责/编号/对比度/配色/雷达刻度全修)。
- 真机测试(真舞《关山酒》)后端全绿。

## 待办(按优先级)
1. 🔴 **真人真钱¥9.9扫码闭环验收**(只Karen手机能做·上线前最后一关)
2. **带货下游**:等Karen淘宝联盟PID→建`taobao_union.py`(keyword→找同款+返佣)。上游认穿搭已好。PRD见 `docs/PRD-带货-穿搭同款-淘宝联盟.md`。转卖砍掉。
3. ✅**专家评审团评价已建成+上线**(舞蹈/艺考/演出3专家·📐测量/🎭观察分区·身韵诚实观察不打分·修好"身韵评不准"):后端`_expert_panel`挂decompose.expert_panel,前端结果页📐/🎭分区渲染,真机验证。评分标准`docs/评分标准-专家评审团-0907.md`。剩:身韵想更准=多帧轨迹(task#5)
4. **实时看角度**(课中·摄像头·demo ai_fitness_coach有未接线上)
5. **我的购买记录页**(orders有数据·无页面)+ 进步曲线接线
6. 记忆重复检测推9.5(单帧→多帧轨迹·task#5)
7. UI小尾:结果页顶栏银→金过渡 / home-v2示例卡标"示例"

## demo & 已知小bug
- **Demo录屏脚本(导演版)**:`docs/demo录屏脚本-60s-导演版-0907.md`(60s·7镜头·量到度当最强钩子)。⚠️ video-gate优化要传脚本**正文**不是文件路径(上轮传路径→跑空)。待Karen定用哪支舞录+配音/字幕。
- ⚠️**小bug(非阻断)**:经 `?demo=<id>` 深链打开结果页后,点某段"慢放"拉到的是**别支舞的口诀**(detailCurPieceIdx没设成该demo舞)。**正常流程(上传→结果→点慢放)不受影响**,是深链预览路径的边界。待修。
- ✅**全流程真机验证**:关山酒 端到端全渲染(八拍卡逐帧角度+专家评审团📐/🎭+故事卡+记忆速记攻略+慢放镜像+口诀浮层)。线上 `/app?demo=d355e63e-185f-41ad-83af-b38ee8dd8f21` 可看。

## 下一步
真机验收付款闭环 → 拿淘宝PID接带货下游 → 专家评审团+身韵。定位=课前(选舞/爆款发现)+课中(跟练/实时角度)+课后(群舞独舞老师审+学生复习),卖给成人爱好者/艺考生/机构。多机位自动拍全班=硬件大版本,SV CEO评4/10建议先跑通单机付费再说。

关联:`docs/PRD-带货-穿搭同款-淘宝联盟.md`
