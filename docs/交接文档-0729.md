# 舞镜 WuJing · 交接文档（0729）

> AI 舞蹈拆解评分教练。接手先读这份 + `PRD-舞镜-0729.md`。

## 1. 基本盘
- **真生产**：https://wujing.mylumee.app （HK 服务器 `47.242.80.65:3006`）
- **后端**：`/www/wujing-api`（FastAPI + uvicorn，systemd 服务 `wujing-api`，**非 git**）
- **前端**：单文件 H5 `/www/wujing-api/static/design-upgrade.html`（服务器直接改静态即生效，**不用重启**）
- **改 Python(server.py/analyze.py 等) 要**：`systemctl restart wujing-api` + `curl http://127.0.0.1:3006/api/health` 验证
- **SSH**：`ssh -i ~/.ssh/id_ed25519 root@47.242.80.65`
- **本地资产**：`~/projects/舞镜/docs`（PRD/mockups/计划，**非 git**）
- **GitHub**：`karentan30/wujing-app`(应用) + `karentan30/wujing-marketing`(营销)——⚠️与本地/服务器脱节，需重新同步

## 2. 模型与密钥（花钱铁律：先给预算+经Karen同意）
env 在 `/www/wujing-api/start.sh`（`env_loader.py` 启动时解析）：
- `DEEPSEEK_API_KEY` ✅ — 三张卡文字+老师点评（≈¥0.002/次，便宜）
- `ARK_API_KEY` ✅ — 豆包（vision评分 + seedream生图）
- `ARK_VISION_EP=ep-20260729155405-5l7dj` ✅ — 豆包 vision 推理接入点（评分用）
- `JWT_SECRET` ✅
- **待Karen填**：Stripe 5个 key（见 `docs/支付接入-待Karen.md`）
- **省钱铁律**：vision 必须 `thinking:disabled`（省30倍token）；图压360px；生图是最大成本坑

## 3. 0729 今天做了什么（新增，真AI核心上线）
- **豆包 vision 真评分**：`vision_score.py`（关思考+压图+6帧并行，~5s/帧）替换了原来的假像素相减。实测65/74分+真问题定位。
- **DeepSeek 三张卡+老师点评**：`cards.py`（`generate_cards` + `generate_coach_note`），注入 `data/dance_kb.json`(38术语)让点评专业。
- **接进** `analyze.py` 的 `run_analysis`：产出 score/dims/problems/highlights/**cards(含coach)**；`server.py` 的 `/api/review` 返回 cards。
- **堵漏洞**：`/api/generate-bg` 加了登录鉴权（原来无鉴权=全网可烧豆包余额，疑似之前300元去向）。
- **舞库入库**：本机 yt-dlp 下爆款→SCP→ffmpeg剪24s→DeepSeek生成 breakdown.json→存 `/www/wujing/{key}/`。已入库7支(kemusan科目三/qingluu只此青绿/babymonster/daomadama刀马刀马/illitmag/luoshen洛神赋/blackpink)。脚本 `/tmp/ingest_dances.py`、`ingest_extra.py`。
- **TEACHER_KEYS 改动态扫目录**：`server.py` 的 `_valid_teacher_keys()` 扫 `/www/wujing/*/`(有reference.mp4+breakdown.json即有效)，以后加舞不用改代码。
- **支付 Stripe 骨架**：`payment.py`（8路由/orders表/免费额度门/微信支付宝桩），待Karen填key。
- **分成后台方案**：`docs/分成接入方案.md`（复用 `~/projects/affiliate`）。

## 4. ⚠️ 已存在资产（别重造！0729教训：动手前先grep现有）
- **富版三卡+慢放** = `design-upgrade.html` 的 `renderDetailCards`/`renderDetailPage`(PIECES数据)：八拍卡定格照网格/歌词/意境/记忆钩子 + 慢放0.5×(`detailPlayClip`)。**比简版强，是真产品**。
- **班级/群舞评分** = `docs/mockups/report_demos/report.html`(AI敦煌舞·全班每人得分·老师端)。
- 其他Jul27资产：`wujing_fixed.html`(144KB整app)、`ai_fitness_coach.html`(健身版demo)、`dance_decompose.html`。

## 5. 进行中 / 下一步
- **结果页重设计**（进行中）：分舞种主题（古典→清雅国风浅底、K-pop→潮酷暗金），样图 `docs/mockups/redesign-genre-themes-0729.html`，正精修到10分。定稿后**接进 design-upgrade.html 富版布局**、按 category 自动切主题、把今天的真内容(评分/点评)接上显示。**别丢内容、别重做，纯升级皮+接线**。
- 待办(PRD backlog)：整合班级评分进线上 / 舞库补齐(唐宫/比心/孔雀/惊鸿/张元英/刘浩存/金晨/欧美明星/拉丁/肚皮舞Shakira) / 群舞逐人 / 健身版 / 登录UI / 支付上线(填key) / 作品广场·打赏·达人分销·带货·演出 / 海外双腿(国风护城河+K-pop走量)。

## 6. 变现（定价已定稿）
免费体验 + ¥9.9/次 + ¥39/月会员 + 机构/班级版；海外Stripe(可复用Lumee/YiYi美国个人号)。飞轮：达人分销挂链接分成 + 服装/化妆品带货 + 品牌赞助 + 打赏 + 作品广场UGC + 线下演出对接。

## 7. Agent 队列
`docs/agent-queue-0729.md`（分成/支付/登录UI/作品广场/群舞/健身，已标抄Lumee哪里+冲突类别）。⚠️改同一文件的agent不并行。群舞/班级已存在=从队列删。
