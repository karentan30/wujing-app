# 舞镜交接文档 · 0806

## 今日完成

### 卡片系统全面升级
**问题**：八拍卡/镜面卡布局"乱"——左右分栏导致帧图只有196px宽，文字6层竞争，口诀叠进大字里

**修复**：
- 改为 **flashcard 布局**：上 100px 信息条（大字+序号+口诀+动作），下方 4 帧全宽横排
- 帧图从 196px → 266px，宽37%，跳舞动作清晰可见
- 修复 `kou_y = key_y + 100` → `key_y + 118`（大字实际高度115px，之前重叠）
- 修复 canvas H 计算错误（用实际 `_hdr_h` 而非常量 100 计算高度）
- 专家评分：3.5/10 → 预计 7/10

**文件**：
- `_live/cards/八拍卡_v3.py`
- `_live/cards/镜面卡_v3.py`

### 卡片 QA 自动检测
`_live/cards/gen_cards.py` 每次生成后自动检查：
- 文件存在且 > 500KB
- 图片是竖版（H > W）
- 高度 > 2000px（未被截断）
- Header 颜色不是纯黑
→ 问题立刻报警，不再静默通过

### 前端卡片显示升级
- 卡片下载区从 emoji（📋🪞🧠）改为**真实缩略图预览**
- 新增 `/api/decompose/{did}/card_thumb/{name}` 轻量缩略图 API
  - 取卡片顶部 800px 裁剪，缩放到 320px 宽
  - JPEG ~7KB vs 原 PNG ~8MB，加载秒速
  - 缓存 24h
- `_live/server.py` line ~734

### 全库卡片重新生成
| 舞蹈 | 状态 |
|------|------|
| 清明雨上 | ✅ 三张卡 QA通过 |
| 群舞·琵琶曲 | ✅ 三张卡 QA通过 |
| 青山烟雨客 | ✅ 三张卡 QA通过 |
| 舞蹈1/2/3 | ✅ 三张卡 QA通过 |
| BLACKPINK/BTS/NewJeans | ⚠️ 暂时隐藏（空壳，无真实视频） |

K-pop 3支：clips/frames 目录都是空的，从没上传过视频。
恢复方法：上传真实视频跑拆解 → 在 design-upgrade.html 取消注释（line ~957）

### PostHog 埋点（已在上一 session 完成）
- 前端：`phc_mBrp44sNSMrWwGJVqmPTNyLAbBQZLqTyMpWrbhbTqzPH`
- 后端：`analytics.py` + `pay.py` 埋 `payment_start` / `payment_success`
- 服务器 `start.sh` 已加 `POSTHOG_KEY` / `POSTHOG_HOST`

---

## 当前产品状态

### 可用功能
- ✅ 上传视频 → AI 拆解 → 生成口诀（歌谣体格式）
- ✅ 生成三张卡（八拍卡/镜面卡/记忆卡）→ 用户下载
- ✅ 卡片前端显示真实缩略图预览
- ✅ 付款（微信 ¥9.9），WJ_FREE_MODE=0（付费模式）
- ✅ PostHog 前后端全链路埋点

### 已知遗留
- **K-pop 3支**：需上传真实视频重新跑拆解
- **个人作品库**：后端 `my_works.py` 完成，前端无入口
- **群舞/班级**：后端 `group_review.py` 完成，前端无入口
- **Demo 视频**：Karen 曾问，待定方向（产品演示 vs 营销短视频）

---

## 服务器部署

```bash
# 服务
systemctl status wujing-api
systemctl restart wujing-api

# 日志
journalctl -u wujing-api -n 50 -f

# 卡片手动重新生成（单支）
cd /www/wujing-api/cards
python3 gen_cards.py <DID> /www/wujing-api/data/<DID>/input.mp4 /www/wujing-api/data/<DID>

# 全库重新生成
bash /tmp/regen_all_cards.sh
```

- 服务器：`47.242.80.65`
- 端口：`3006`
- 数据目录：`/www/wujing-api/data/`
- 卡片脚本：`/www/wujing-api/cards/`

---

## 开发规范（今日确立）

### 卡片改动三层防线
1. **改完立刻本地生成一张看**：`python3 gen_cards.py` 肉眼检查
2. **push 前派 QA agent**：读代码 + 看生成图，有问题不推
3. **自动 QA 检测**：`gen_cards.py` 内置，每次生成后自动验证

---

## 本地 → 服务器部署

```bash
# 单文件
scp _live/server.py root@47.242.80.65:/www/wujing-api/server.py

# 前端
scp _live/static/design-upgrade.html root@47.242.80.65:/www/wujing-api/static/design-upgrade.html

# 卡片脚本
scp _live/cards/八拍卡_v3.py _live/cards/镜面卡_v3.py root@47.242.80.65:/www/wujing-api/cards/

# 重启
ssh root@47.242.80.65 "systemctl restart wujing-api"
```

---

## Git

- Branch: `auto-decompose-mediapipe-0730`
- 最新 commit: `898124a` feat: 卡片全面升级 + 前端修复 0806
- Remote: `https://github.com/karentan30/wujing-app.git`
