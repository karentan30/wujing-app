# 舞镜交接文档 · 0806

## 今日完成

### 1. 安全+合规修复
- **XSS escaping**：用户输入全部 `_esc()` 转义，防注入
- **去掉假倒计时**：`lp-c.html` 移除虚假"活动倒计时"（合规红线）

### 2. Bug修复
- `DC_STEPS` 变量提升到外层作用域（原来是 `var` 在 if 块里，部分浏览器拿不到）
- `home-v2.html` viewport 加 `viewport-fit=cover`，手机刘海屏不裁边

### 3. 静态资源
- `_live/static/design-upgrade.html` 新增静态副本（CDN/静态服务器直出用）

---

## 昨日完成（0805）回顾
- 八拍卡/故事卡/记忆卡分享升级（缩略图预览+iOS修复+fonts.ready+toast）
- 新增"分享成绩"Canvas卡片（评分结果页，1080×1350，金色渐变）

---

## 待做
- 视频背景替换（Karen提供 2031690254.mp4，橘色暖调，待确认替换哪个区域）
- 部署：`scp _live/design-upgrade.html root@47.242.80.65:/www/wujing/design-upgrade.html`

## 服务器
- HK：47.242.80.65，`/www/wujing/`
- 重启：`ssh root@47.242.80.65 "systemctl restart wujing"`
