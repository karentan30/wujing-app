# 舞镜 DAY 0 · 首日4小时行动表
**日期**: 2026-08-08  
**目标**: 3个CVSS≥8漏洞 + 支付验收完成  
**负责**: Claude Code (2.5h) + Karen (0.5h)

---

## 🔴 URGENT: 代码安全修复（2.5小时）

### 11:00 - 11:15 | 任务1: S5 文件上传RCE漏洞

**文件**: `/Users/karen/projects/舞镜/_live/server.py`

**检查清单**:
```bash
# 1. 确认 _check_video_magic 函数存在
grep -A 15 "def _check_video_magic" ~/projects/舞镜/_live/server.py

# 2. 查找 /api/upload 端点
grep -n "/api/upload" ~/projects/舞镜/_live/server.py
```

**修复代码** (定位到 /api/upload 端点，检查是否有magic byte校验):

```python
# 改前（风险）:
@app.post("/api/upload")
async def upload(video: UploadFile = File(...)):
    content = await video.read()
    # 直接保存，无校验

# 改后（安全）:
@app.post("/api/upload")
async def upload(video: UploadFile = File(...)):
    content = await video.read()
    if not _check_video_magic(content):  # ← 加这行
        raise HTTPException(415, "Only video files allowed")
    # 保存...
```

**验收命令**:
```bash
cd ~/projects/舞镜/_live
python -m pytest -k "test_upload_security" -v  # 如果有单元测试
# 或手动测试：
curl -F "video=@/etc/hosts" http://localhost:3006/api/upload
# 期望返回 415 Unsupported Media Type
```

---

### 11:15 - 12:00 | 任务2: S11 XSS漏洞 × 2处

**文件1**: `/Users/karen/projects/舞镜/_live/class_join.html`  
**文件2**: `/Users/karen/projects/舞镜/_live/class_teacher.html`

**Step 1: 创建全站 _esc() 函数** (5分钟)

在 `design-upgrade.html` 找到函数区块，加入:

```javascript
// 全站转义函数（复用已有逻辑）
window._esc = function(str) {
  if (!str) return '';
  return str
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;')
    .replace(/'/g, '&#x27;');
};
```

**Step 2: 修复 class_join.html:276** (3分钟)

```html
<!-- 改前 -->
<div id="rNote"></div>
<script>
$('rNote').innerHTML = '老师评语：<br>'+s.teacher_note;  // 危险
</script>

<!-- 改后 -->
<div id="rNote"></div>
<script>
$('rNote').innerHTML = '老师评语：<br>'+_esc(s.teacher_note);  // 安全
</script>
```

**Step 3: 修复 class_teacher.html:207-215** (3分钟)

```html
<!-- 改前 -->
<div>${m.nickname} - ${m.real_name}</div>

<!-- 改后 -->
<div>${_esc(m.nickname)} - ${_esc(m.real_name)}</div>
```

**验收命令**:
```bash
# 检查是否所有innerHTML赋值都用了_esc
grep -n "innerHTML.*=" ~/projects/舞镜/_live/class_join.html \
  ~/projects/舞镜/_live/class_teacher.html | grep -v "_esc"
# 返回为空即通过 ✅

# 或手动测试（注入恶意昵称）
curl -X POST http://localhost:3006/api/register \
  -d '{"nickname":"<script>alert(1)</script>"}' \
# 浏览器查看该昵称，不应执行脚本
```

---

### 12:00 - 12:30 | 任务3: T5 支付鉴权顺序

**文件**: `/Users/karen/projects/舞镜/_live/pay.py`

**当前问题** (搜索这个):
```python
@app.post("/api/pay/wechat/create")
async def wechat_create(body: PayRequest):  # ← body校验先于auth
    # 无token这里返回422，应返回401
```

**修复代码**:

```python
# 改前
@app.post("/api/pay/wechat/create")
async def wechat_create(body: PayRequest):
    user = get_current_user(request.headers.get("authorization"))
    # 问题: body校验在auth之前

# 改后
from fastapi import Header

@app.post("/api/pay/wechat/create")
async def wechat_create(
    authorization: str = Header(None),
    body: PayRequest = Body(...)
):
    # Step 1: Auth check FIRST
    if not authorization:
        raise HTTPException(status_code=401, detail="Missing token")
    
    try:
        user = get_current_user(authorization)
    except:
        raise HTTPException(status_code=401, detail="Invalid token")
    
    # Step 2: Body validation AFTER auth
    if body.dance_id not in VALID_DANCES:
        raise HTTPException(status_code=422, detail="Invalid dance")
    
    # ... rest of logic
```

**验收命令**:
```bash
# 无token请求应返回401（不是422）
curl -X POST http://localhost:3006/api/pay/wechat/create \
  -H "Content-Type: application/json" \
  -d '{"invalid": "body"}' \
  -w "\nStatus: %{http_code}\n"
# 期望: 401 Unauthorized
```

---

### 12:30 - 12:45 | 任务4: T9-CTO subprocess无超时

**文件**: `/Users/karen/projects/舞镜/_live/auto_decompose.py`

**搜索所有subprocess.run调用** (30秒):
```bash
grep -n "subprocess.run" ~/projects/舞镜/_live/auto_decompose.py
```

预期看到4处调用。改法:

```python
# 改前（无超时，可无限阻塞）
result = subprocess.run(cmd, capture_output=True, text=True)

# 改后（5分钟超时，防OOM）
result = subprocess.run(
    cmd,
    capture_output=True,
    text=True,
    timeout=300  # ← 加这行
)
```

**4处位置** (逐一搜：`_dur`, `_grab`, `_clip` 函数):
- [ ] `_dur()` ffmpeg调用
- [ ] `_grab()` ffmpeg调用  
- [ ] `_clip()` ffmpeg调用
- [ ] `_run()` 其他subprocess

**验收**: `grep "timeout=" auto_decompose.py | wc -l` → 应返回4或以上

---

### 12:45 - 13:30 | 任务5: T9-Ops /api/health性能排查

**诊断流程** (自检5步):

```bash
# Step 1: 本机响应时间 (应<100ms)
time curl http://localhost:3006/api/health

# Step 2: 服务器资源占用
ssh root@47.242.80.65 "top -b -n 1 | head -5"
# 检查: CPU%<80, MEM<800MB

# Step 3: 代码审查（health endpoint无DB查询）
grep -A 5 '@app.get("/api/health")' ~/projects/舞镜/_live/server.py
# 应该只有 return {"status": "ok"} 类似的纯内存操作

# Step 4: 如果检查都正常但线上还是慢
# 改为添加轻量级 /api/ping（专供监控）
@app.get("/api/ping")
def ping():
    return {"ok": True}  # 纯内存，1ms级

# Step 5: 重启服务验证
ssh root@47.242.80.65 "systemctl restart wujing-api && sleep 2 && \
  for i in {1..5}; do time curl -s http://localhost:3006/api/health; done"
# 所有响应应<200ms
```

**修复要点**（如果需要）:
- 移除health endpoint里的任何DB.execute()调用
- 确保DATA_DIR不在systemd资源笼子限制内（ffmpeg在主进程外）

---

## 🟡 支付链路真人验收（0.5小时）— Karen专场

**Karen的任务** (手机操作，无需代码):

| 步骤 | 操作 | 期望结果 | 所需时间 |
|-----|------|---------|----------|
| 1 | 打开 `https://wujing.mylumee.app` | 首页加载成功，无报错 | 30秒 |
| 2 | 拖入一支本地舞蹈视频（MP4) | 文件上传进度条显示0-100% | 1分钟 |
| 3 | 等待拆解完成（预期60-120秒） | 八拍卡/故事卡/记忆卡全显 | 2分钟 |
| 4 | 点"看完整拆解"或"付款"按钮 | 弹窗显示"¥9.9" | 30秒 |
| 5 | 选"微信"或"支付宝" | 二维码出现（真实渠道） | 30秒 |
| 6 | **用真实手机扫码支付** | ✅ 支付完成（可能需要验证码） | 3分钟 |
| 7 | 等待回调 | ✅ 拆解结果全展 / toast通知 | 1分钟 |
| 8 | 切"我的作品"tab | 🎯 刚才上传的舞蹈在列表 | 30秒 |
| 9 | **截图完整流程** | 发给Claude Code | 30秒 |

**验收成功的标志** ✅:
```
✅ 支付前能看到预览（建立信任）
✅ 支付二维码真实可扫
✅ 支付完成有明确反馈
✅ 拆解结果在支付后立即解锁
✅ 我的作品库有数据
```

**万一失败**：
- 如果拆解卡404 → 报错信息，继续改代码
- 如果支付返回错误 → 截图错误，检查token/商户配置
- 如果"我的作品"为空 → 检查登录状态 & /api/my_works 接口

---

## 📋 汇总与交接

**完成后的状态**：

| 项 | 状态 | 签字 |
|----|------|------|
| S5 RCE修复 + 验收 | ✅ | Claude Code |
| S11 XSS修复 × 2 + 验收 | ✅ | Claude Code |
| T5 鉴权顺序修复 + 验收 | ✅ | Claude Code |
| T9-CTO subprocess超时 + 验收 | ✅ | Claude Code |
| T9-Ops health性能诊断 + 修复 | ✅ | Claude Code |
| **支付真人验收通过** | ✅ | **Karen签字** |

**最后一步** (Claude Code执行，Karen授权后):
```bash
ssh root@47.242.80.65 "
  sed -i 's/Environment=\"WJ_FREE_MODE=1\"/Environment=\"WJ_FREE_MODE=0\"/' \
    /etc/systemd/system/wujing-api.service.d/override.conf
  systemctl daemon-reload
  systemctl restart wujing-api
  sleep 2
  curl -s http://localhost:3006/api/health | grep -q ok && echo 'PAYMENT MODE ENABLED ✅'
"
```

---

**预计完成时间**: 14:30 （4小时闭环）  
**下一步**: DAY 1 开始产品UX优化

