# 舞镜 QA审计发现与快速修复方案 — 2026-08-10

## 📊 审计结果汇总

| 来源 | P0 | P1 | 总体评分 | 状态 |
|------|----|----|---------|------|
| 交互设计 | 5 | 5 | 4.5/10 | 修复中 |
| UX清晰度 | 5 | 7 | 5.2/10 | 修复中 |
| 移动端 | 5 | 3 | 5.5/10 | 修复中 |
| **总计** | **15** | **15** | **5.1/10** | **进行中** |

---

## 🚀 第一轮修复（最关键）

### 交互设计 P0 (5个)

#### P0.1 支付按钮无spinning loader
**位置**：design-upgrade.html 行 4507-4541 (`payCreate` 函数)  
**现状**：显示"正在生成二维码…"文本，但无视觉旋转反馈  
**修改方案**：
```javascript
// 行4497改为：
document.getElementById('payQrHint').innerHTML='<span style="animation:spin 0.6s linear infinite">⟳</span> 正在生成二维码…';
```
**状态**：✅ 待修

---

#### P0.2 fetch无30秒超时保护
**位置**：行 4507-4541 (payCreate) 和 4550 (payPoll)  
**现状**：fetch无超时设置，30秒网络慢挂死无反馈  
**修改方案**：
```javascript
// 添加AbortController超时
const abortController = new AbortController();
const timeoutId = setTimeout(() => abortController.abort(), 30000);
fetch('api/pay/'+channel+'/create', {
  ...
  signal: abortController.signal
}).catch(err => {
  if(err.name === 'AbortError') {
    toast('请求超时（>30s），请检查网络后重试');
  }
  ...
})
```
**状态**：✅ 待修

---

#### P0.3 错误用alert()
**位置**：行 4535（已改good）+ 其他地方  
**现状**：使用 toast() 已经很好，检查全文其他alert  
**状态**：✅ 检查完成 - 已用toast

---

#### P0.4 支付成功无确认modal
**位置**：行 4553-4570 (payPoll 支付成功后)  
**现状**：直接显示✓，600ms后自动关闭进入dcLoading，用户不清楚  
**修改方案**：
```javascript
// 添加成功确认modal
function showPaymentSuccess(){
  var m = document.createElement('div');
  m.style.cssText = `
    position: fixed; top: 50%; left: 50%; 
    transform: translate(-50%, -50%);
    background: white; padding: 32px; border-radius: 12px;
    box-shadow: 0 12px 48px rgba(0,0,0,0.15);
    z-index: 10000; text-align: center; min-width: 320px;
  `;
  m.innerHTML = `
    <div style="font-size: 48px; margin-bottom: 16px;">✅</div>
    <h2 style="margin: 0 0 12px; font-size: 22px; color: #1a1a1a;">支付成功！</h2>
    <p style="margin: 0 0 24px; color: #666; font-size: 14px;">您的拆解已解锁，正在生成完整分解卡…</p>
    <button onclick="this.parentElement.remove();" style="
      width: 100%; padding: 12px; background: #3d9e82; color: white;
      border: none; border-radius: 8px; font-size: 15px; font-weight: 600;
      cursor: pointer;
    ">查看拆解卡</button>
  `;
  document.body.appendChild(m);
  setTimeout(() => m.remove(), 3000);
}
```
**状态**：✅ 待修

---

#### P0.5 按钮文案不清
**位置**：行 3800 + 3743-3744  
**现状**：
- "¥9.9 拆开这支舞"（不清楚能得到什么）
- "开始拆解 · ¥9.9"（语序奇怪）

**修改方案**：
```javascript
// 行3743改为：
var priceLabel = WJ_DISCOUNT ? 
  (_isZH()?'¥7.9 解锁完整分解卡（8折）':'$1.59 Unlock full breakdown') : 
  (_isZH()?'¥9.9 解锁完整分解卡':'$1.99 Unlock full breakdown');

// 行3800改为：
btn.textContent = _isZH()?'¥9.9 解锁完整分解卡':'$1.99 Unlock full breakdown';
```
**状态**：✅ 待修

---

### UX清晰度 P0 (5个)

#### UX P0.1 首屏标题用行业术语
**位置**：设计文档中的首屏标题  
**改法**：改为"上传舞蹈视频 → AI秒反馈"  
**状态**：✅ 文案待更新

---

#### UX P0.2 首屏缺¥9.9价格锚点 (最关键)
**改法**：首屏显著位置加价格卡"完整拆解 ¥9.9/支"  
**状态**：✅ 待加

---

#### UX P0.3-P0.5 定价/术语/流程不清
**改法**：见上述文案统一  
**状态**：✅ 待更新

---

### 移动端 P0 (5个)

#### Mobile P0.1-P0.4 触控目标 + 字号 + 安全区
**文件**：design-upgrade.html 中的 CSS  
**改法**：
- 分类按钮改 min-height: 44px
- 所有文字改 ≥12px
- 底部链接预留 padding-bottom: 60px (iOS安全区)
- 按钮改为 44x120px 最小

**状态**：✅ CSS待改

---

## 📋 修改清单（按优先级）

- [ ] **P0.1** 支付按钮添加spinning loader
- [ ] **P0.2** fetch添加30秒超时保护
- [ ] **P0.3** 错误处理验证
- [ ] **P0.4** 支付成功modal
- [ ] **P0.5** 按钮文案改为"¥9.9解锁完整分解卡"
- [ ] **UX P0.1** 首屏标题改为"上传舞蹈 → AI秒反馈"
- [ ] **UX P0.2** 首屏加价格锚点
- [ ] **Mobile P0.1-4** CSS修改（触控/字号/安全区）
- [ ] 全量测试 (支付/超时/错误/成功各场景)
- [ ] 部署到生产

---

## ✅ 验收标准

所有P0修完后：
- 支付流程测试 ✓
- 超时保护测试 ✓
- 移动端375px实测 ✓
- 无console.log/TODO残留 ✓
- 上线清单全绿 ✓

---

**开始时间**：2026-08-10 18:40  
**目标完成**：2026-08-10 22:00 (3小时20分)  
**责任人**：CEO团队 + QA验证
